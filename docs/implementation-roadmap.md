# MTI Alert Implementation Roadmap

## Document Status

- Version: `0.6`
- Status: `Active`
- Last Updated: `2026-09-05`

## Active Phase

- `Phase 4 - Hardening And Expansion`

## Roadmap Execution Gate

- Every roadmap checklist step must include a matching challenge/verification activity before it can be marked complete.
- Challenge/verification must test the changed behavior, contract, workflow path, authorization path, or failure path relevant to that step.
- Completion evidence must be written into the owning phase `Challenge / Verification` section or a referenced supporting verification document before status changes are declared.
- A checked checklist item without recorded verification evidence is treated as `not yet complete`.

## Phase 0 - Documentation Baseline

### Status

- `Completed`

### Objective

Establish the baseline source-of-truth documentation for the MTI Alert server so implementation can proceed with explicit product, workflow, data, and API guidance.

### Source Documents

- `docs/project-plan.md`
- `docs/product-principles.md`
- `docs/functional-specification.md`
- `docs/technical-implementation-plan.md`
- `docs/database-schema-specification.md`
- `docs/openapi.yaml`
- `docs/open-questions-and-challenges.md`
- `docs/template-policy-schema.md`

### Supporting Documents

- `docs/architecture-decisions.md`
- `docs/testing-strategy.md`
- `docs/deployment-and-environment.md`
- `docs/windows-agent-client-specification.md`
- `docs/backend-module-breakdown.md`
- `docs/phase-1-execution-plan.md`

### Checklist

- `[x]` Documentation baseline: define the project vision and MVP scope.
- `[x]` Documentation baseline: define product principles and operating constraints.
- `[x]` Documentation baseline: define functional domains and key workflows.
- `[x]` Documentation baseline: define conceptual backend architecture.
- `[x]` Documentation baseline: define conceptual database schema.
- `[x]` Documentation baseline: define the initial OpenAPI contract.
- `[x]` Documentation baseline: record unresolved questions and challenges.
- `[x]` Documentation refinement: align the mandatory documents with user decisions.
- `[x]` Documentation refinement: add supporting references where ambiguity remained high.
- `[x]` Documentation refinement: add template policy schema support.
- `[x]` Documentation refinement: align the OpenAPI contract with phased implementation boundaries and reusable contract patterns.
- `[x]` Documentation refinement: add backend module breakdown guidance for implementation planning.
- `[x]` Documentation refinement: add a Phase 1 execution plan for implementation sequencing and verification.

### Output

- A complete baseline documentation package in `docs/`.
- Repository entry documents should guide humans and AI agents into the authoritative `docs/` set.
- Supporting schema references may be added when they reduce implementation ambiguity.

### Challenge / Verification

- Verification target: required documentation files exist and form a coherent baseline for implementation.
- Challenge: key architecture decisions were narrowed, but several implementation details remain open and are tracked in `docs/open-questions-and-challenges.md`.
- Evidence: mandatory baseline documents were created and refined against explicit user decisions on `2026-07-06`, then extended with supporting baseline references, a template policy schema reference, repository entry documentation guidance, a more implementation-ready phased OpenAPI contract, backend module breakdown guidance, and a dedicated Phase 1 execution plan on `2026-07-06`.

## Phase 1 - Core Backend Foundation

### Status

- `Completed`

### Objective

Implement the core backend foundation for authentication, role scope enforcement, organization data, communication management, and audience resolution.

### Source Documents

- `docs/project-plan.md`
- `docs/functional-specification.md`
- `docs/technical-implementation-plan.md`
- `docs/database-schema-specification.md`
- `docs/openapi.yaml`

### Supporting Documents

- `docs/architecture-decisions.md`
- `docs/testing-strategy.md`
- `docs/deployment-and-environment.md`
- `docs/template-policy-schema.md`
- `docs/windows-agent-client-specification.md`
- `docs/backend-module-breakdown.md`
- `docs/phase-1-execution-plan.md`

### Checklist

- `[x]` Application baseline: scaffold the backend runtime, config loading, database bootstrap placeholder, request validation utility, shared error handling, and health endpoint.
- `[x]` Auth foundation: define the concrete LDAP or AD login flow.
- `[x]` Auth foundation: define session or token strategy for admin APIs.
- `[x]` Auth foundation: implement `login`, `logout`, and `me` endpoints.
- `[x]` Authorization: define local role mapping and site or area scope model.
- `[x]` Authorization: implement backend scope enforcement guards.
- `[x]` Authorization: challenge unauthorized access scenarios and expected errors.
- `[x]` Database foundation: implement versioned migrations and initial schema for auth, organization, employee, and device tables.
- `[x]` Organization data: defer the HR sync ingestion contract from Phase 1 and record the non-blocking go-live decision. `Deferred To Post-Go-Live`
- `[x]` Organization data: implement reference endpoints for sites, areas, departments, and sections.
- `[x]` Organization data: implement sync-safe read models for employees and devices.
- `[x]` Communication drafts: implement draft create, get, update, and duplicate rules.
- `[x]` Communication drafts: enforce template-locked field validation in the API layer.
- `[x]` Audience preview: implement target resolution for site, area, employee, group, and device target types.
- `[x]` Audience preview: implement publish preview output with recipient counts and channel plan.
- `[x]` Frontend integration: replace mock auth and draft services with backend-backed service calls.
- `[x]` Frontend integration: validate that admin flows still work against the new contract.

### Output

- Running backend foundation with documented API coverage.
- Initial backend modules exist for auth, authorization, organization references, communication drafts, and audience preview.

### Challenge / Verification

- Milestone 0 scaffold builds a separate `backend/` runtime foundation without changing current business behavior.
- Milestone 1 and Milestone 2 baseline currently use LDAP-backed admin authentication, opaque in-memory bearer sessions, role metadata, and a documented global-scope placeholder until organization-scoped records arrive.
- Versioned migration support now exists through `backend:migrate` and `backend:migrate:status`, with the initial Phase 1 foundation schema recorded in `backend/migrations/0001_phase1_foundation.up.sql`.
- Milestone 3 and Milestone 4 baseline now expose `GET /reference/organization`, `GET /reference/sites`, `GET /reference/areas`, `GET /reference/departments`, `GET /reference/sections`, `GET /employees`, and `GET /devices`.
- Live PostgreSQL connectivity was verified against the target `ictMTIAlertHub` database on `2026-07-07`, and the initial schema migration has now been applied for `users`, `user_scopes`, `sites`, `areas`, `departments`, `sections`, `employees`, and `devices`.
- Build and typecheck pass.
- API contract matches implementation.
- Authorization scope is challenged with unauthorized access scenarios.
- Representative authenticated smoke tests for organization and device endpoints return `200`, and an unauthenticated `GET /devices` request returns `401`.
- Table existence and zero-row baseline were verified after migration application, confirming that current empty-state endpoint responses now come from unseeded tables rather than missing schema.
- Admin login, template selection, draft authoring, draft detail, draft update, draft duplication, and audience preview flows now consume backend-backed services instead of relying on mock notification state or static targeting references.
- Employee directory, device list, and dashboard summary metrics now consume backend-backed read models for employees and devices, while unsupported mutate actions remain explicitly disabled at the UI layer.
- Publish preview output is challenged with representative target combinations.
- `2026-07-08`: targeted API verification confirms `POST /communications`, `GET /communications/{communicationId}`, `PATCH /communications/{communicationId}`, `POST /communications/{communicationId}/duplicate`, and `POST /communications/{communicationId}/audience-preview` all work against the live database-backed runtime.
- `2026-07-08`: template-locked field enforcement is challenged by attempting to remove a mandatory template channel, and the API correctly returns `422 MANDATORY_CHANNEL_REQUIRED`.
- `2026-07-08`: saved-group audience resolution is now backed by `audience_groups` and `audience_group_members`, and representative `Group` plus `Device` target previews return expected recipients with zero preview warnings.
- Go-live fast-track decision on `2026-07-07`: HR or organization baseline ingestion is not treated as a go-live blocker and is explicitly deferred until after the first live release unless operational needs force it back into scope.
- `2026-07-08`: frontend-to-backend CORS compatibility was updated for local Phase 1 admin verification origins on `4173` and `5173`, and direct preflight plus authenticated login requests from the frontend origin to the backend verification runtime returned the expected headers and `200` login response.
- `2026-07-09`: the local development CORS allowlist was expanded to also accept common frontend dev origins on `3000`, `3001`, and `8080` for both `localhost` and `127.0.0.1`, preventing admin UI login failures caused by origin mismatch while preserving the explicit allowlist model.
- `2026-07-08`: frontend smoke verification remains partially constrained by the current browser automation environment because cross-port localhost requests from the tool fail with `net::ERR_FAILED`, but the Phase 1 admin contract was still challenged through successful frontend build, backend-backed service wiring, and direct runtime checks against the same frontend origin and backend target.

## Phase 2 - Delivery Orchestration

### Status

- `In Progress`

### Objective

Implement communication publication, scheduling, recipient snapshots, delivery job orchestration, and routine reminder policy distribution, with the first live release focused on Windows Agent connectivity before WhatsApp provider integration.

### Source Documents

- `docs/functional-specification.md`
- `docs/technical-implementation-plan.md`
- `docs/database-schema-specification.md`
- `docs/openapi.yaml`
- `docs/open-questions-and-challenges.md`

### Supporting Documents

- `docs/architecture-decisions.md`
- `docs/testing-strategy.md`
- `docs/deployment-and-environment.md`
- `docs/template-policy-schema.md`
- `docs/windows-agent-client-specification.md`
- `docs/backend-module-breakdown.md`
- `docs/phase-2-agent-minimum-slice-plan.md`
- `docs/reminder-hybrid-ux.md`
- `docs/ohih-program-module-blueprint.md`

### Checklist

- `[x]` Slice 2.1 Publish guardrails: require confirmed preview and reject publish attempts for non-draft communications.
- `[x]` Slice 2.1 Publish guardrails: validate `publishMode`, `scheduledAt`, `recurrenceRule`, `timezone`, `executionMode`, and `validUntil` combinations before state changes.
- `[x]` Slice 2.1 Publish guardrails: reject template-policy violations that would make execution behavior ambiguous at publish time.
- `[x]` Slice 2.2 Immediate publish acceptance: implement `publish now` state transition from `Draft` into an execution-ready backend state.
- `[x]` Slice 2.2 Immediate publish acceptance: persist the operator-confirmed publish request together with effective publish metadata needed by downstream orchestration.
- `[x]` Slice 2.3 Scheduled publication foundation: persist one-time scheduled publication with the authoritative schedule timestamp and timezone context.
- `[x]` Slice 2.3 Scheduled publication foundation: implement cancel validation and the state transition for scheduled or active communications through `POST /communications/{communicationId}/cancel`.
- `[x]` Slice 2.4 Recurring schedule foundation: persist recurring schedule definition, execution mode, version, and cancellation state on the server.
- `[x]` Slice 2.4 Recurring schedule foundation: distinguish server-generated recurring execution from approved Windows Agent local routine reminder policy execution.
- `[x]` Slice 2.5 Recipient snapshot foundation: generate immutable recipient snapshots for device-targeted and contact-targeted recipients at publish time.
- `[x]` Slice 2.5 Recipient snapshot foundation: persist template version, workflow version reference, and effective policy snapshot with the execution record.
- `[x]` Slice 2.6 Delivery job foundation: create delivery jobs per recipient and per selected channel from the publish snapshot.
- `[x]` Slice 2.6 Delivery job foundation: persist delivery attempt records, bounded retry metadata, and normalized channel lifecycle status transitions.
- `[x]` Slice 2.7 Windows Agent minimum trust: implement persisted device session issuance and validation for `POST /agent/session`.
- `[x]` Slice 2.7 Windows Agent minimum trust: implement authenticated `POST /agent/heartbeat` with device-session ownership checks and device health refresh.
- `[x]` Slice 2.8 Windows Agent reconciliation: implement `GET /agent/messages` as contract-valid pending-message reconciliation, including safe empty-state behavior.
- `[x]` Slice 2.8 Windows Agent reconciliation: implement idempotent `Displayed` and `Read` evidence ingestion for `POST /agent/messages/{messageId}/displayed` and `POST /agent/messages/{messageId}/read`.
- `[x]` Slice 2.8 Windows Agent reconciliation: implement `POST /agent/messages/{messageId}/response` using workflow-aware response validation when the published communication requires a response.
- `[x]` Slice 2.9 Realtime compatibility boundary: implement `POST /agent/realtime/negotiate` so the server can return concrete negotiation metadata instead of placeholders.
- `[x]` Slice 2.9 Reminder policy sync: implement `GET /agent/reminder-policies` plus policy invalidation semantics for approved local routine reminders.
- `[x]` Slice 2.9 Reminder policy sync: implement `POST /agent/reminder-policies/{policyId}/events` for local occurrence and sync-state reporting.
- `[ ]` Slice 2.10 WhatsApp connector boundary: implement outbound provider interface, callback ingestion contract, and delivery-state normalization. Deferred until after the first Windows Agent go-live release.
- `[ ]` Slice 2.10 WhatsApp connector boundary: map actual provider delivery and read receipts into MTI Alert delivery lifecycle states without over-claiming `Read`. Deferred until after the first Windows Agent go-live release.
- `[x]` Slice 2.11 Frontend publish integration: replace mock publish and cancel service calls with backend-backed integrations on the admin notification flow.
- `[x]` Slice 2.11 Frontend publish integration: replace mock delivery and device-monitoring service calls with backend-backed visibility aligned to the Phase 2 contract.
- `[x]` Slice 2.11 Frontend publish integration: validate the admin publish and delivery monitoring flow against the new Phase 2 contract.

### Output

- Backend supports end-to-end Windows Agent communication dispatch orchestration for the first live release.
- Windows Agent delivery orchestration, reconciliation, and routine reminder sync are ready to serve the initial client go-live path.
- WhatsApp provider delivery orchestration is explicitly deferred until after the first Windows Agent go-live release.
- Routine reminder policies can be synchronized to Windows Agent for bounded local execution without changing server ownership of schedule lifecycle.

### Challenge / Verification

- Scheduled and immediate communications are both verified.
- Approved local routine reminders remain functional across disconnect and reconnect scenarios.
- Failed delivery paths are challenged.
- Delivery state rollup is validated for multi-channel recipients.
- Disconnected-agent recovery is challenged with pending-message reconciliation scenarios.
- `2026-07-08`: Slice 2.1 publish guardrails are now enforced through `POST /communications/{communicationId}/publish`, including draft-only publish, required preview confirmation, scheduling field-combination checks, recurring reminder execution validation, and publish-time template policy consistency checks.
- `2026-07-08`: focused runtime verification against a dedicated backend instance confirmed `422 PREVIEW_CONFIRMATION_REQUIRED`, `422 TIMEZONE_REQUIRED`, `422 TEMPLATE_POLICY_REFERENCE_REQUIRED`, `409 COMMUNICATION_NOT_DRAFT`, and `409 PUBLISH_ACCEPTANCE_NOT_READY`, while `POST /communications/{communicationId}/audience-preview` still returned `200` with resolved recipients for the same draft under test.
- `2026-07-08`: Slice 2.2 now accepts `publishMode = Now` through `POST /communications/{communicationId}/publish`, transitions the communication from `Draft` into `Queued`, and persists an `Immediate` record in `communication_schedules` together with the validated publish request snapshot and actor metadata needed by downstream orchestration.
- `2026-07-08`: Slice 2.3 now accepts one-time scheduled publication through `POST /communications/{communicationId}/publish`, persists timezone-aware schedule metadata in `communication_schedules`, and supports `POST /communications/{communicationId}/cancel` for `Scheduled`, `Queued`, `Sending`, and `Active` communications by transitioning them to `Cancelled` and deactivating the active schedule record.
- `2026-07-08`: Slice 2.4 now accepts recurring publication through `POST /communications/{communicationId}/publish`, persists `Recurring` schedule records with `execution_mode`, validity metadata, and cancellation state in `communication_schedules`, and uses the persisted `execution_mode` to distinguish `ServerGenerated` recurring execution from `AgentLocalRoutine` foundation behavior.
- `2026-07-08`: focused runtime verification against a dedicated backend instance on `BACKEND_PORT=4012` confirmed `publishMode = Now` returns `200` with communication status `Queued`, `publishMode = Scheduled` returns `200` with communication status `Scheduled`, `publishMode = Recurring` returns `200` with communication status `Scheduled`, scheduled cancellation returns `200` with communication status `Cancelled`, and `communication_schedules` persisted the expected `Immediate`, `Scheduled`, and `Recurring` records with the expected active-state transitions.
- `2026-07-08`: Slice 2.5 now writes immutable `communication_recipients` snapshots during publish acceptance for `Device`, `Employee`, and `ContactEndpoint` recipients, carrying template version, workflow reference, and effective policy snapshot data with the execution record.
- `2026-07-08`: Slice 2.6 now writes `delivery_jobs`, initial `delivery_attempts`, and append-only `delivery_events` from the publish snapshot, including bounded retry metadata and cancellation-to-failed normalization for pending jobs when a scheduled communication is cancelled.
- `2026-07-08`: focused runtime verification against a dedicated backend instance on `BACKEND_PORT=4013` confirmed immediate, scheduled, and recurring publish acceptance produced the expected recipient snapshot mix, created per-channel delivery jobs, persisted initial attempts and `Queued` events, and transitioned pending scheduled jobs into `Failed` plus `AdminApi` failure events when the communication was cancelled.
- Compatibility baseline added on `2026-07-08`: the backend now exposes `POST /agent/session`, `POST /agent/realtime/negotiate`, `POST /agent/heartbeat`, `GET /agent/messages`, `GET /agent/reminder-policies`, `POST /agent/messages/{messageId}/displayed`, `POST /agent/messages/{messageId}/read`, `POST /agent/messages/{messageId}/response`, and `POST /agent/reminder-policies/{policyId}/events`.
- Compatibility baseline expanded on `2026-07-08`: the backend now also exposes `GET /agent/realtime-hub` as the first working `SSE` hub transport, emits `connected`, `messages.snapshot`, and `messages.available` events, and pushes queued Windows Agent delivery jobs to currently connected devices while preserving reconciliation through `GET /agent/messages`.
- Current limitation for that baseline: the first compatible hub still reuses the persisted agent session token, currently emits the active message set for a device instead of a strictly delta-only stream, and local routine reminder sync remains limited to device-bound Windows Agent recurring recipients.
- `2026-07-08`: delivery scope was reprioritized for the first live release so Windows Agent connectivity is the release-critical path, while `Slice 2.10 WhatsApp connector boundary` is intentionally deferred until after the desktop client go-live milestone.
- `2026-07-08`: the current first-release blocker review is captured in `docs/go-live-checklist.md`, with the concrete realtime hub endpoint and the final operator publish path called out as the remaining release-critical gaps for the Windows Agent go-live path.
- `2026-07-08`: the concrete realtime hub endpoint gap is now closed through a first compatible `SSE` slice, so the remaining first-release blocker is the final operator publish path if the admin UI is included in the release scope.
- `2026-07-08`: the admin notification UI now uses backend-backed `publish` and `cancel` actions on the detail flow, including immediate and scheduled publish confirmation, while delivery monitoring and device-support visibility remain the unfinished frontend portion of `Slice 2.11`.
- Verification evidence on `2026-07-08`: `npm run backend:typecheck` passed, `npm run backend:build` passed, and `npm run backend:migrate:dev` applied `0005_phase2_communication_schedules.up.sql` plus `0006_phase2_delivery_foundation.up.sql` after implementing publish, cancel, recipient snapshot, and delivery foundation persistence.
- `2026-07-08`: Slice 2.7 now persists `POST /agent/session` trust in `device_sessions`, rotates the device token on session refresh, renews expiry on authenticated agent usage, and uses persisted device ownership to validate `POST /agent/heartbeat` before updating device freshness metadata.
- `2026-07-08`: focused runtime verification against a dedicated backend instance on `BACKEND_PORT=4014` confirmed a registered device receives `200` from `POST /agent/session`, exactly one persisted `device_sessions` row remains for that device after session refresh, `POST /agent/heartbeat` returns `204` and updates `devices.last_heartbeat_at` plus `status`, and the replaced token is rejected with `401 UNAUTHORIZED`.
- `2026-07-08`: Slice 2.8 now serves `GET /agent/messages` from persisted `delivery_jobs` plus `communication_recipients`, records idempotent `Displayed` and `Read` delivery events from the Windows Agent, and accepts workflow-validated `POST /agent/messages/{messageId}/response` by transitioning the message into `Responded` while updating recipient response and acknowledgement state.
- `2026-07-08`: focused runtime verification against a dedicated backend instance on `BACKEND_PORT=4015` with `LDAP_ALLOWED_GROUPS=''` confirmed an admin can create and publish a Windows Agent-targeted communication, the target device receives exactly one pending message from `GET /agent/messages`, duplicate `Displayed` and `Read` submissions remain `204` and create only one lifecycle event each, `POST /agent/messages/{messageId}/response` returns `200`, the delivery job transitions to `Responded`, recipient state becomes `Responded` plus `Acknowledged`, and the message no longer appears in reconciliation after response submission.
- `2026-07-08`: Slice 2.9 now materializes `AgentLocalRoutine` recurring reminder policies into `agent_reminder_policies`, records local reminder evidence in `agent_reminder_events`, persists realtime negotiation compatibility rows in `device_realtime_connections`, and rejects `AgentLocalRoutine` publish attempts that do not resolve to at least one device-bound Windows Agent recipient.
- `2026-07-08`: focused runtime verification against a dedicated backend instance on `BACKEND_PORT=4016` with `LDAP_ALLOWED_GROUPS=''` confirmed an admin can publish a recurring `AgentLocalRoutine` reminder, `POST /agent/realtime/negotiate` returns a concrete `connectionUrl` plus reusable session-backed access token and persists a `Connected` realtime row, `GET /agent/reminder-policies` returns the synchronized policy, `POST /agent/reminder-policies/{policyId}/events` records a `Triggered` occurrence, and cancelling the communication removes the target `policyId` from subsequent reminder-policy sync responses while leaving the persisted policy row inactive.
- `2026-07-09`: Slice 2.11 now exposes `GET /communications/{communicationId}/deliveries` as the thin Phase 2 admin visibility baseline, returning paged delivery jobs together with persisted recipient snapshots and recent delivery events derived from `communication_recipients`, `delivery_jobs`, and `delivery_events`.
- `2026-07-09`: the admin notification detail flow now uses backend delivery visibility for the `Recipients`, `Delivery Logs`, and acknowledgement summary tabs instead of placeholder data, while richer device monitoring and reporting remain intentionally deferred.
- `2026-07-09`: focused verification for Slice 2.11 passed with `npm run backend:typecheck`, `npm run backend:build`, and `npm run build`, and a dedicated runtime on `BACKEND_PORT=4018` confirmed a create-preview-publish sequence returns `Queued`, then `GET /communications/{communicationId}/deliveries` returns at least one recipient snapshot, one delivery job, and a persisted `Queued` delivery event detail.
- `2026-07-09`: a Windows Agent prototype smoke validation on `BACKEND_PORT=4019` confirmed the first-release startup and recovery path end to end: `POST /agent/session` succeeded, `POST /agent/realtime/negotiate` returned fresh `connectionId` values across reconnect, `GET /agent/realtime-hub` emitted `connected` plus `messages.snapshot`, `POST /agent/heartbeat` refreshed device health, publish `Now` emitted `messages.available` to the active stream, and after reconnect the same pending communication remained available through both `messages.snapshot` and `GET /agent/messages`.
- `2026-07-09`: `POST /agent/realtime/negotiate` no longer hardcodes `localhost` in `connectionUrl`; the backend now derives the realtime base URL from the inbound request host by default and supports `BACKEND_PUBLIC_BASE_URL` as an explicit deployment override when a reverse proxy or external host name must be advertised to Windows Agent clients.
- `2026-07-15`: realtime negotiation is now hardened against loopback misconfiguration in deployment env. If `BACKEND_PUBLIC_BASE_URL` is accidentally left as `localhost` or another loopback host, but the inbound negotiate request arrives on a non-loopback host such as `10.60.10.59`, the backend now prefers the inbound request host for `connectionUrl` so remote Windows Agent clients do not try to open SSE against themselves. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, and a dedicated runtime on `BACKEND_PORT=4027` started with `BACKEND_PUBLIC_BASE_URL=http://localhost:4027`; a live `POST /agent/session` plus `POST /agent/realtime/negotiate` request sent with `Host: 10.60.10.59:4027` returned `connectionUrl=http://10.60.10.59:4027/agent/realtime-hub?...`.

## Phase 3 - Response Workflow And Monitoring

### Status

- `Completed`

### Objective

Implement recipient response workflows, overdue behavior, monitoring, and audit-ready reporting on top of the persisted delivery foundation from Phase 2.

Phase 3 is the bridge between delivery execution and operator visibility:

- turn persisted delivery evidence into operator-facing response and monitoring views
- ensure required-response communications move through explicit lifecycle states
- make overdue and follow-up behavior operationally visible
- prepare representative audit evidence before broader hardening in Phase 4

### Source Documents

- `docs/functional-specification.md`
- `docs/technical-implementation-plan.md`
- `docs/database-schema-specification.md`
- `docs/openapi.yaml`

### Supporting Documents

- `docs/architecture-decisions.md`
- `docs/testing-strategy.md`
- `docs/template-policy-schema.md`
- `docs/windows-agent-client-specification.md`
- `docs/backend-module-breakdown.md`

### Execution Direction

#### Slice 3.1 Workflow Baseline

- Scope:
  - workflow definition source-of-truth is available to communication drafting and response evaluation
  - workflow option constraints are validated consistently during submission
- Current status:
  - draft selection and workflow snapshots are already used by the runtime
  - managed seed loading now reconciles the canonical workflow baseline during backend bootstrap
- Done when:
  - operators can only use valid workflow definitions and valid option sets
  - runtime no longer depends on undocumented workflow assumptions

#### Slice 3.2 Response Ingestion And Semantics

- Scope:
  - Windows Agent response submission persists workflow result, actor context, and optional note
  - `response implies ack` behavior is enforced when configured by the workflow
- Current status:
  - Windows Agent and compatible-channel response submission baselines are in place
  - actor context and `response implies ack` are already persisted across both ingestion paths
- Done when:
  - required-response messages move from `AwaitingResponse` to `Responded`
  - monitoring endpoints can explain who responded, what option was chosen, and when it happened

#### Slice 3.3 Monitoring And Reporting Baseline

- Scope:
  - communication-level, recipient-level, dashboard, and tracked-content rollup views are backend-backed
  - device, employee, and contact-endpoint recipient contexts are distinguishable in monitoring
- Current status:
  - baseline monitoring endpoints and reports are implemented
  - dashboard and reports are already consuming persisted backend data
- Done when:
  - operators can answer `what was sent`, `to whom`, `through which channel`, and `what happened next`
  - monitoring counts reconcile against `communication_recipients`, `delivery_jobs`, and `delivery_events`

#### Slice 3.4 Overdue And Recipient-Only Follow-Up

- Scope:
  - required-response recipients can transition into `Overdue`
  - MVP follow-up remains recipient-only and must not escalate to new recipients automatically
- Current status:
  - persisted overdue state and one-time Windows Agent recipient-only re-alert baseline are implemented
- Done when:
  - overdue recipients are visible in detail monitoring and dashboard rollups
  - follow-up behavior is recorded explicitly enough to challenge and support operational review

#### Slice 3.5 Auditability

- Scope:
  - publish, cancel, override rejection, response, and representative state transitions produce explicit audit evidence
  - audit evidence is queryable enough for operator/support investigation
- Current status:
  - append-only audit log persistence and admin retrieval baseline are implemented
- Done when:
  - representative lifecycle events are not only inferable from delivery tables but intentionally recorded as audit history
  - support teams can reconstruct a communication lifecycle without reading raw infrastructure logs

#### Slice 3.6 Frontend Alignment

- Scope:
  - remove monitoring/reporting mocks that would hide real backend state
  - ensure admin views show persisted response, overdue, and delivery rollups
- Current status:
  - dashboard, reports, delivery visibility, and response summary are already backend-backed
- Done when:
  - operator UI is a faithful read-model of backend monitoring state for the MVP release path

### Checklist

- `[x]` Workflow management: implement workflow definition CRUD or managed seed loading.
- `[x]` Workflow management: implement workflow option validation rules.
- `[x]` Response handling: implement response submission for Windows Agent and compatible channel responses.
- `[x]` Response handling: enforce `response implies ack` semantics in the backend.
- `[x]` Response handling: store actor context as optional audit metadata where available.
- `[x]` Monitoring: implement communication-level monitoring endpoints.
- `[x]` Monitoring: implement recipient-level monitoring endpoints with device and contact distinctions.
- `[x]` Monitoring: implement dashboard summary endpoints and aggregation queries.
- `[x]` Monitoring: implement delivery and response state rollups for tracked content types.
- `[x]` Overdue handling: implement timeout evaluation and recipient-only follow-up triggers.
- `[x]` Auditability: implement audit logging for publish, cancel, override rejection, response, and state transitions.
- `[x]` Frontend integration: replace mock reports, dashboards, and response summary services with backend-backed calls.

### Output

- Backend supports monitored response workflows and operational reporting.
- Reporting and monitoring are backed by persisted delivery, response, and audit records.
- Operators can identify required-response progress, overdue recipients, and recipient-only follow-up behavior from backend-backed views.
- Phase 3 response workflow, monitoring, overdue, auditability, and compatible-channel response baselines are complete for the MVP release path.

### Challenge / Verification

- Response state transitions are validated.
- Overdue cases are challenged.
- Dashboard counts reconcile with source records.
- Audit records are challenged against representative lifecycle events.
- For every closed slice, verification should name the exact endpoint, runtime port, and persisted state that changed.
- Phase 3 evidence should always reconcile three layers together:
  - admin-facing endpoint or UI result
  - persisted source rows or aggregate query result
  - lifecycle event sequence that explains why the state is correct
- `2026-07-09`: Phase 3 is now active because Windows Agent startup, realtime push, reconnect, and thin admin delivery visibility were already validated for the first go-live path.
- `2026-07-09`: the backend now exposes `GET /communications/{communicationId}/responses` as the first admin response-monitoring endpoint, returning paged recipient responses derived from persisted `Responded` delivery events for the communication.
- `2026-07-09`: manual `workflowId` selection on communication drafts now also sets `requiresResponse`, preventing `RESPONSE_NOT_REQUIRED` conflicts when operators create response-required communications without relying on template defaults.
- `2026-07-09`: the admin notification detail page now includes a backend-backed `Responses` tab that lists recipient name, response option, actor, and response time using the new Phase 3 response endpoint together with persisted recipient snapshots.
- `2026-07-09`: focused verification passed with `npm run backend:typecheck`, `npm run build`, and a dedicated runtime smoke on `BACKEND_PORT=4020` that created a Windows Agent communication with workflow `11111111-1111-1111-1111-111111111111`, published it to `device-mti-ops-01`, submitted agent response option `safe`, and confirmed `GET /communications/{communicationId}/responses` returned the persisted response plus actor context with `totalItems = 1`.
- `2026-07-09`: the backend now exposes `GET /dashboard/overview` as the first dashboard summary baseline, and the admin dashboard stat cards use this endpoint for active communications, pending recipients, delivered jobs, responded recipients, failed jobs, and overdue response counts instead of local mock aggregation.
- `2026-07-09`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, and a dedicated runtime smoke on `BACKEND_PORT=4021` that authenticated an admin session and confirmed `GET /dashboard/overview` returned non-error aggregate metrics (`activeCommunications = 10`, `recipientsPending = 5`, `deliveredCount = 10`, `respondedCount = 5`, `failedCount = 3`, `overdueResponses = 0`).
- `2026-07-09`: `GET /communications/{communicationId}/deliveries` now serves as the first recipient-level monitoring baseline, returning `Device`, `Employee`, and `ContactEndpoint` recipient contexts together with `deviceId`, `deviceIdentifier`, `hostname`, and `channelEndpoint` where available, and the admin notification detail `Recipients` tab now surfaces those references directly for operator monitoring.
- `2026-07-09`: focused verification passed with `npm run backend:typecheck`, `npm run build`, and a dedicated runtime smoke on `BACKEND_PORT=4022` that published one device-targeted Windows Agent communication plus one employee-targeted WhatsApp communication, then confirmed `GET /communications/{communicationId}/deliveries` returned a `Device` recipient with `deviceId = 401317fc-fb05-4020-b69f-15c7bd6d90d6`, `deviceIdentifier = device-mti-ops-01`, `hostname = MTI-OPS-01`, and a `ContactEndpoint` recipient with `channelEndpoint = +628000000001`.
- `2026-07-09`: the backend now exposes `GET /dashboard/content-type-rollups` as the first tracked-content reporting baseline, grouping persisted communication, recipient, workflow, and delivery state into per-`communicationType` delivery and response rollups without requiring a derived reporting table.
- `2026-07-09`: the admin `Reports` page now uses backend-backed content-type rollups instead of mock department, site, and channel chart data, and `GET /communications` now includes persisted `category`, `createdAt`, `recipientsCount`, and `ackCount` summary fields so report history and dashboard notification summaries no longer default to placeholder zero counts.
- `2026-07-09`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, and a dedicated runtime smoke on `BACKEND_PORT=4023` that published one `Alert` plus one `News` communication to `device-mti-ops-01`, reported `Displayed`, `Read`, and workflow `Responded` for the `Alert`, then confirmed `GET /dashboard/content-type-rollups` returned six tracked content-type rows including `Alert` rollups with `readCount = 11` and `respondedCount = 7`, while `GET /communications` returned the created `Alert` summary with `recipientsCount = 1` and `ackCount = 1`.
- `2026-07-09`: overdue handling now persists `communication_recipients.response_state = Overdue`, records `delivery_events.event_type = Overdue`, and stores `follow_up_triggered_at` when recipient-only timeout evaluation triggers a one-time Windows Agent re-alert for the same recipient and delivery job.
- `2026-07-09`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run backend:migrate`, `npm run build`, and a dedicated runtime smoke on `BACKEND_PORT=4024` that temporarily set the seeded workflow timeout to `0`, published a response-required `Alert` to `device-mti-ops-01`, triggered `GET /agent/messages`, and then confirmed `GET /communications/{communicationId}/deliveries` returned a `Device` recipient with `responseState = Overdue`, an `Overdue` delivery event, a re-queued `Pending` delivery job for recipient-only follow-up, and updated dashboard overdue counts.
- `2026-07-09`: the backend now exposes `GET /audit-logs` as the first audit trail baseline, backed by append-only `audit_logs` rows for publish acceptance, cancel acceptance, template override rejection, agent response recording, overdue transitions, and recipient-only follow-up queue events; the admin `Audit Logs` page now reads this backend endpoint instead of mock seed data.
- `2026-07-09`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run backend:migrate`, `npm run build`, and a dedicated runtime smoke on `BACKEND_PORT=4025` that forced a template override rejection, published a response-required Windows Agent communication, submitted an agent response, cancelled the communication, and then confirmed `GET /audit-logs` returned `TemplateOverrideRejected`, `PublishCommunication`, `CommunicationStatusChanged`, `RecordResponse`, `RecipientResponseStateChanged`, and `CancelCommunication` entries tied to the exercised lifecycle.
- `2026-07-09`: the backend now exposes `GET /workflows` as the workflow-definition source of truth for admin authoring and runtime workflow resolution, returning rows from `response_workflows` plus ordered `response_workflow_options`; the workflow service also enforces definition validation rules for blank options, duplicate option keys, invalid free-text combinations, and non-positive timeout settings before a definition can be used by the runtime.
- `2026-07-09`: the admin create/edit draft flows now load workflow definitions from `GET /workflows`, require a real `workflowId` when `requireAck` is enabled, and no longer hardcode workflow `11111111-1111-1111-1111-111111111111` inside the compose/update payload path.
- `2026-07-14`: backend bootstrap now runs managed workflow seed loading before the HTTP server is exposed, reconciling the canonical `Critical Acknowledgement` and `Reminder Confirmation` definitions plus their ordered response options so workflow authoring no longer depends on a one-time migration seed remaining untouched.
- `2026-07-14`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, and `node backend/tmp/phase3-workflow-seeds-smoke.mjs`; the smoke deliberately drifted the seeded workflow name and deleted the reminder option, then confirmed a dedicated runtime on `BACKEND_PORT=4026` restored canonical `/workflows` output with `totalItems = 2`, `Critical Acknowledgement` option count `3`, and `Reminder Confirmation` option label `Acknowledged`.
- `2026-07-14`: the backend now exposes `POST /communications/{communicationId}/deliveries/{deliveryJobId}/response` as the thin compatible-channel response ingestion baseline for non-Windows-Agent delivery jobs, reusing persisted workflow snapshots to validate the selected response option, recording `Responded` delivery events, updating recipient response state, and applying `response implies ack` semantics before full provider callback modules exist.
- `2026-07-14`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, and `node backend/tmp/phase3-compatible-channel-response-smoke.mjs`; the smoke started a dedicated runtime on `BACKEND_PORT=4027`, published a `Reminder` to an employee over `WhatsApp`, confirmed `GET /communications/{communicationId}/deliveries` returned a `ContactEndpoint` recipient with `channelEndpoint = +628000000001`, submitted response option `done` through the new delivery-response endpoint, and then confirmed both `GET /communications/{communicationId}/responses` and `GET /audit-logs` reflected the persisted `WhatsApp` response evidence.

## Phase 4 - Hardening And Expansion

### Status

- `In Progress`

### Objective

Prepare the platform for broader rollout and future channels.

### Source Documents

- `docs/project-plan.md`
- `docs/technical-implementation-plan.md`
- `docs/open-questions-and-challenges.md`
- `docs/openapi.yaml`

### Supporting Documents

- `docs/architecture-decisions.md`
- `docs/testing-strategy.md`
- `docs/software-acceptance-test.md`
- `docs/deployment-and-environment.md`
- `docs/operational-runbook.md`
- `docs/security-and-access-model.md`
- `docs/integration-contracts.md`
- `docs/windows-agent-client-specification.md`
- `docs/windows-agent-rollout-blueprint.md`
- `docs/windows-agent-rollout-stage-1-contract.md`
- `docs/windows-agent-updater-service-spec.md`
- `docs/windows-agent-wix-msi-packaging-plan.md`
- `docs/wellness-program-execution-checklist.md`

### Checklist

- `[ ]` Channel expansion: define and document the email connector contract.
- `[ ]` Channel expansion: implement email delivery orchestration if included in scope.
- `[ ]` Channel expansion: define and document the digital signage connector contract.
- `[ ]` Channel expansion: implement digital signage orchestration if included in scope.
- `[x]` Windows Agent deployment hardening: define the hybrid installer, startup, update, and remote uninstall model for broader desktop rollout.
- `[x]` Windows Agent deployment hardening: define the Stage 1 backend contract for rollout intent retrieval and updater status reporting.
- `[x]` Windows Agent deployment hardening: define the Stage 2 technical specification for the local updater service.
- `[x]` Windows Agent deployment hardening: define the Stage 3 WiX/MSI packaging baseline and installer component map.
- `[x]` Reminder UX: expose recurring reminder authoring with recurrence rule, timezone, execution mode, and validity-window controls in the admin experience.
- `[x]` Reminder UX: expose operator guidance and publish confirmation for `ServerGenerated` versus `AgentLocalRoutine` reminder execution.
- `[x]` Reminder UX: expose reminder policy activity and reconciled reminder evidence in the admin monitoring flow.
- `[x]` Observability: harden logging, tracing, and operational alerting.
- `[x]` Observability: improve connector-health and realtime-hub diagnostics.
- `[x]` Security: harden session handling, token rotation, and agent trust controls.
- `[x]` Security: review directory integration security settings and environment handling.
- `[x]` Device visibility: enrich registered device read models with Active Directory-backed active-user context while preserving device-centric trust and ownership semantics.
- `[x]` Reporting: expand historical reporting and export capability.
- `[ ]` Open expansion track: finalize the remaining post-MVP shape for `Wellness Programs`, especially active-user audit visibility and post-routine usefulness or need feedback. The MVP boundary itself is now locked in `docs/wellness-program-execution-checklist.md`.
- `[x]` Operations: define deployment, rollback, and incident-response runbook guidance.
- `[x]` Documentation: update source docs and supporting docs for any post-MVP contract changes.

### Output

- Hardened platform with clearer path to post-MVP expansion.
- Operational and security guidance is documented for production-oriented rollout.

### Challenge / Verification

- New channel contracts are documented.
- Operational failure recovery is challenged.
- Security-sensitive actions are re-verified.
- Production-oriented deployment and rollback assumptions are challenged.
- `2026-07-14`: Phase 4 is now active with a desktop-first go-live hardening slice; the current release decision is to keep `WindowsAgent` enabled for the first live path while `WhatsApp`, `Email`, and `Digital Signage` remain deferred unless explicitly enabled for a controlled environment.
- `2026-08-26`: the Windows Agent deployment baseline is now explicitly documented as hybrid. `GPO`, `Intune`, `SCCM`, or `PDQ` remain the bootstrap and break-glass paths, while routine update and uninstall may be orchestrated by the running agent through a dedicated updater component rather than direct tray-app self-replacement. Verification passed through synchronized updates in `docs/architecture-decisions.md`, `docs/windows-agent-client-specification.md`, `docs/deployment-and-environment.md`, and `docs/open-questions-and-challenges.md`, plus content inspection confirming the rollout, trust-boundary, and fallback guidance.
- `2026-08-26`: a dedicated supporting blueprint now exists in `docs/windows-agent-rollout-blueprint.md`, turning the approved rollout direction into a concrete implementation baseline for `MSI` packaging, updater-service responsibilities, startup registration, backend rollout intent, uninstall flow, fallback endpoint-management, and security guardrails. Verification passed through document review confirming the blueprint stays aligned with `ADR-10`, the Windows Agent client specification, deployment baseline, and the current Phase 4 hardening scope.
- `2026-08-26`: the first rollout backend contract slice is now documented. `docs/openapi.yaml` now defines `GET /agent/rollout-intent` and `POST /agent/rollout-status`, `docs/database-schema-specification.md` now describes `agent_release_packages`, `agent_rollout_intents`, and `agent_rollout_status_events`, `docs/technical-implementation-plan.md` now includes rollout intent retrieval plus updater status reporting under the Windows Agent module, and the implementation notes are narrowed further in `docs/windows-agent-rollout-stage-1-contract.md`. Verification passed through cross-document review confirming the endpoint shapes, state vocabulary, and persistence direction stay aligned with the approved hybrid rollout model.
- `2026-08-26`: the Stage 2 updater-service implementation baseline is now documented in `docs/windows-agent-updater-service-spec.md`. The spec defines the Windows Service shape, `LocalSystem` baseline, authenticated named-pipe IPC, `%ProgramData%\\MTI Alert\\Updater` staging and state layout, tray-app quiesce flow, silent `MSI` upgrade or repair or uninstall execution, local failure categories, and the rule that backend status reporting remains owned by the tray app rather than the service itself. Verification passed through document review confirming the updater-service spec stays aligned with the rollout blueprint, Stage 1 contract, Windows Agent client baseline, and deployment guardrails.
- `2026-08-26`: the Stage 3 installer baseline is now documented in `docs/windows-agent-wix-msi-packaging-plan.md`. The plan defines the machine-wide `WiX` and `MSI` baseline, `%ProgramFiles%` and `%ProgramData%` separation, tray-app and updater-service component groups, Windows Service registration, scheduled-task registration, major-upgrade and repair behavior, uninstall retention rules, and installer verification expectations. Verification passed through document review confirming the packaging plan stays aligned with the rollout blueprint, updater-service specification, client packaging baseline, and deployment environment assumptions.
- `2026-08-26`: the first real updater-service and installer implementation slice now exists in the Windows Agent repository. A new `MTI.Alert.Updater` Windows Service project was added with machine-wide `%ProgramData%\\MTI Alert\\Updater` path handling, file logging, authenticated named-pipe request intake, rollout request validation, and persisted `active-rollout.json` plus `last-rollout.json` state capture. The existing `WiX` installer project now publishes the updater payload, installs `MTI.Alert.Updater.exe` under `C:\Program Files\MTI\MTI.Alert.Agent\Updater\`, registers the `MTI.Alert.Updater` Windows Service, and creates the updater machine-data directories under `CommonAppData`. Verification passed with `dotnet build .\\MTI.Alert.Agent\\Updater\\MTI.Alert.Updater\\MTI.Alert.Updater.csproj`, direct `dotnet publish` of `MTI.Alert.Agent.csproj` for the MSI payload, and `dotnet build .\\MTI.Alert.Agent\\Installer\\MTI.Alert.Agent.Setup\\MTI.Alert.Agent.Setup.wixproj -c Release`, which produced `MTI.Alert.Agent.Setup.msi` successfully after the WPF publish and WiX directory-authoring issues were resolved.
- `2026-08-26`: the next rollout implementation slice now closes the first automatic staging gap between accepted intent and eventual install. The tray-app updater pipe client build blocker was fixed, the updater service now runs a background execution worker that detects accepted `Upgrade` and `Repair` rollouts, downloads the approved package into `%ProgramData%\\MTI Alert\\Updater\\Staging\\`, verifies the payload `sha256`, verifies the embedded Authenticode signer thumbprint against rollout metadata, and then persists `Staged` with the local package path or `Failed` with an explicit staging error code. Verification passed with `npm run backend:typecheck`, `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release`, `dotnet build .\\MTI.Alert.Agent\\Updater\\MTI.Alert.Updater\\MTI.Alert.Updater.csproj -c Release`, and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-08-26`: rollout visibility is now extended one step further on the agent side. The tray app now reads the updater's persisted machine-wide state from `%ProgramData%\\MTI Alert\\Updater\\State\\active-rollout.json` and `last-rollout.json`, reports new updater states such as `Downloading`, `Staged`, and `Failed` back to `POST /agent/rollout-status`, and keeps the local dispatch cache aligned so the same rollout is not pointlessly re-forwarded while progress is already underway. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`, including new focused coverage for updater state-file selection.
- `2026-08-26`: the updater now advances staged `Upgrade` and `Repair` actions through real silent MSI execution instead of stopping at package staging. The service now validates version rules against the installed tray executable, persists `InstallPending` and `Installing`, attempts to quiesce the running tray process, runs `msiexec /i "<packagePath>" /qn /norestart`, records installer exit code plus detected installed version, and then attempts tray restart through the `MTI Alert Agent` scheduled task with a direct executable fallback. The tray app status bridge now also forwards `installedVersion`, `startupRegistered`, restart outcome, and installer exit code from updater state back to the backend. Verification passed with `dotnet build .\\MTI.Alert.Agent\\Updater\\MTI.Alert.Updater\\MTI.Alert.Updater.csproj -c Release --no-restore`, `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore`, and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-08-26`: the MSI packaging baseline now closes the scheduled-task registration gap. The WiX installer now includes the `WixToolset.Util.wixext` extension and uses elevated quiet custom actions to create the `MTI Alert Agent` logon scheduled task during install and repair, then remove it during uninstall. Installer evidence collection and the MSI validation runbook now also check for scheduled-task presence alongside the updater service and machine-data directories. Verification passed with `dotnet build .\\MTI.Alert.Agent\\Installer\\MTI.Alert.Agent.Setup\\MTI.Alert.Agent.Setup.wixproj -c Release`, which produced a successful MSI build after the scheduled-task authoring was added.
- `2026-08-27`: the scheduled-task lifecycle validation now passes end to end by enforcing creation and removal through the updater Windows Service rather than MSI custom actions. On service start, the updater ensures the `MTI Alert Agent` logon task exists; on service stop (including MSI uninstall), the updater ensures the task is removed. Verification evidence: `C:\\ProgramData\\MTI Alert\\Updater\\Logs\\updater-20260827.log` includes `Ensuring startup task registration on service start`, followed by `Ensuring startup task removal on service stop`, and `schtasks /Query /TN "MTI Alert Agent"` after uninstall returns `ERROR: The system cannot find the file specified.`
- `2026-08-28`: a thin server-side rollout helper now exists for controlled device upgrade testing before a full admin rollout UI is implemented. `backend/src/scripts/create-agent-rollout.ts` can upsert approved `agent_release_packages`, deactivate any prior active rollout for the target device, and insert a fresh `agent_rollout_intents` row from explicit package metadata, while `package.json` now exposes `npm run backend:create-agent-rollout:dev -- ...` for local execution. Verification passed with `npm run backend:typecheck` and a dry run against device `MTI-NB-373`, which resolved the online device successfully and produced the expected rollout preview payload without mutating data.
- `2026-08-28`: the package-signing preparation workflow is now cleaner for both local lab testing and corporate code-signing rollout. A new `mti.alert.agent/Installer/prepare-agent-rollout-package.ps1` helper now signs an existing `MSI` from a selected Windows certificate-store identity or inspects an already-signed package, reads `ProductVersion` directly from the `MSI`, computes `sha256`, validates Authenticode presence, and emits the ready-to-run backend rollout command. The existing `sign-local-rollout-test.ps1` now delegates to that general helper for backwards-compatible local self-signed testing, while `docs/operational-runbook.md` and `docs/deployment-and-environment.md` now document the rule that environment-specific config must be baked into the `MSI` before signing and that the exact signed file must be published unchanged. Verification passed first with a `-SkipSign` execution against the current `MTI.Alert.Agent.Setup.msi`, which produced the expected `1.0.2` version metadata plus rollout command output for a signed package without rewriting the artifact, and then with a real `CurrentUser\My` corporate code-signing certificate (`0DCBA67CE24D09DCC98EFFC6E5F85146C1B30630`) after fixing EKU enumeration compatibility under PowerShell `StrictMode`, producing a `Valid` Authenticode signature and updated rollout metadata for the same `1.0.2` package.
- `2026-08-28`: the operator workflow for controlled Windows Agent rollout is now easier to follow. A new wrapper `mti.alert.agent/Installer/invoke-agent-rollout.ps1` now presents the flow as three explicit stages: prepare the signed package metadata, publish the exact signed MSI to the final URL, and create the rollout intent in backend. The wrapper can stop after preparation for a publish handoff or continue with `-ApplyRollout` to call the existing backend helper directly. Verification passed with a preparation-only run against the current signed `1.0.2` MSI, plus an `-ApplyRollout` workflow review confirming the generated backend arguments stay aligned with the existing `backend:create-agent-rollout:dev` helper contract.
- `2026-08-28`: the local publish gap in that rollout workflow is now closed for backend-driven validation. `backend/src/modules/agent/controller/register-agent-routes.ts` now serves files from `backend/local-packages/` through `GET /agent/packages/local/{fileName}`, and `mti.alert.agent/Installer/invoke-agent-rollout.ps1` now copies the prepared signed MSI into that folder automatically whenever the package URL resolves to the backend-local route. Verification passed with `npm run backend:typecheck`, a preparation-plus-publish wrapper run that copied `MTI.Alert.Agent.Setup.msi` into `backend/local-packages/`, and a matching SHA-256 check confirming the published file hash stayed `53A7BC98674B2C6DB40AABD4554A5057BB6A9E09E28C9ADA97EAC2CFFCF4423D`. Runtime note: an already-running backend process must be restarted before the new route becomes reachable on `:4019`.
- `2026-08-29`: the next rollout hardening slice now removes the tray-app fallback-state escape hatch and tightens the intended service-owned update path. `mti.alert.agent/Updater/MTI.Alert.Updater/Services/UpdaterPipeServer.cs` now creates the updater named pipe with explicit ACLs for `LocalSystem`, `Builtin\\Administrators`, and local `Authenticated Users`, while `mti.alert.agent/Infrastructure/Updater/UpdaterPipeClient.cs` now fails fast on pipe authorization or I/O errors instead of writing rollout state directly into `%ProgramData%`. Verification passed with `dotnet build .\\mti.alert.agent\\Updater\\MTI.Alert.Updater\\MTI.Alert.Updater.csproj -c Release`, `dotnet build .\\mti.alert.agent\\MTI.Alert.Agent.csproj -c Release`, and `dotnet build .\\mti.alert.agent\\Installer\\MTI.Alert.Agent.Setup\\MTI.Alert.Agent.Setup.wixproj -c Release`.
- `2026-08-29`: a controlled bootstrap-and-retry package set is now prepared for validating the repaired normal rollout path on `MTI-NB-373`. A signed bootstrap package `backend/local-packages/MTI.Alert.Agent.Setup.1.0.4.msi` was produced with hash `67B8E1C6131B4F80D5B31318E98002A4A5A5DF398CA27606A8D484F547AAC833`, and a signed follow-up rollout package `backend/local-packages/MTI.Alert.Agent.Setup.1.0.5.msi` was produced with hash `8165155261A0E3D0025A5C278401D23CB31EDDF47ED7F21716EEA22ECE8A72AB`. Backend rollout intent `cdd2e945-3203-400a-94c1-2e6f462d708c` is now active for target version `1.0.5` via `http://127.0.0.1:4019/agent/packages/local/MTI.Alert.Agent.Setup.1.0.5.msi`. Verification passed with `prepare-agent-rollout-package.ps1` signing runs for both package versions, hash verification of the archived published files under `backend/local-packages`, and a successful direct execution of `backend/src/scripts/create-agent-rollout.ts --apply` for device `MTI-NB-373`. Runtime note: end-to-end success is still pending the one-time bootstrap install of `1.0.4` on the endpoint so the repaired updater-service IPC can take over routine backend-driven upgrades.
- `2026-08-29`: the repaired normal rollout path has now been exercised successfully end-to-end on `MTI-NB-373`. After a one-time bootstrap install of `backend/local-packages/MTI.Alert.Agent.Setup.1.0.4.msi`, the installed tray app `1.0.4` forwarded rollout intent `cdd2e945-3203-400a-94c1-2e6f462d708c` to the updater service, the updater staged `backend/local-packages/MTI.Alert.Agent.Setup.1.0.5.msi`, executed silent `msiexec`, restarted the tray app, and the backend later observed device version `1.0.5` with status `Online`. Verification passed with Windows Installer log inspection for bootstrap `1.0.4`, direct inspection of installed binaries under `C:\Program Files\MTI\MTI.Alert.Agent\`, updater log inspection under `%ProgramData%\MTI Alert\Updater\Logs\`, persisted updater state inspection showing `installedVersion = 1.0.5` and `installerExitCode = 0`, and backend device-query confirmation that `MTI-NB-373` now reports `agentVersion = 1.0.5`.
- `2026-08-29`: that same end-to-end validation also exposed a follow-up correctness bug in post-upgrade retry handling. When the same rollout intent was re-forwarded after success, the updater downgraded the local rollout state to `Failed` with `VERSION_RULE_REJECTED` because the installed version already equaled the target version. `MTI.Alert.Agent/Updater/MTI.Alert.Updater/Services/RolloutExecutionWorker.cs` now narrows the upgrade version guard so it rejects only versions strictly greater than the approved target, preventing same-version post-success retries from producing a false failure. Verification is partially complete through source patch review; a follow-up package/build is still required before that status-fidelity fix is live on endpoints.
- `2026-08-29`: the first admin-web rollout control slice now exists across `Settings > Desktop Agent` and `Devices`. The backend now exposes `GET /devices/rollout-packages/local`, `POST /devices/rollout-packages/upload`, and `POST /devices/{deviceId}/rollouts`; `backend/src/modules/devices/service/device-action-service.ts` now supports browser-originated local package upload, local package discovery, and rollout dry-run or apply behavior; the admin frontend `src/routes/_app.settings.tsx` now treats package upload and package registry review as a global Desktop Agent settings concern; and `src/routes/_app.devices.tsx` now restricts the rollout dialog to selecting one of those globally registered packages for a single target device. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, plus an authenticated upload smoke against `http://127.0.0.1:4019/devices/rollout-packages/upload` that stored an MSI into `backend/local-packages` and returned inspected package metadata ready for rollout. Runtime note: backend-side package inspection currently falls back to hash-only metadata if PowerShell signature inspection is unavailable in the runtime process, so the rollout dialog still allows manual signer-thumbprint entry when needed.
- `2026-08-29`: the frontend rollout slice has now been exercised end-to-end against a real browser session and a real device. Using the admin `Devices` page, an authenticated operator session selected device `MTI-NB-373`, entered rollout metadata for `http://127.0.0.1:4019/agent/packages/local/MTI.Alert.Agent.Setup.1.0.6.msi`, completed the UI dry-run preview, and applied rollout intent `e531bbbd-67e6-4cdb-8da8-90b05b2942b9` targeting version `1.0.6`. Verification passed with browser-driven login plus form submission, backend query confirmation that the new intent is active, persisted updater state inspection showing `currentState = Succeeded`, `installedVersion = 1.0.6`, and `installerExitCode = 0`, filesystem verification that `C:\Program Files\MTI\MTI.Alert.Agent\MTI.Alert.Agent.exe` now reports product version `1.0.6`, and a live `POST /agent/heartbeat` verification against `http://127.0.0.1:4019` proving that the patched agent-backend sync path now refreshes `public.devices.agent_version` from `1.0.5` to `1.0.6` for `MTI-NB-373`.
- `2026-08-30`: Windows Agent pending-message reconciliation is now bounded on the server side to reduce stale backlog replay. `backend/src/modules/agent/service/agent-service.ts` now returns only Windows Agent delivery jobs whose schedules are active, already inside their `valid_from` window, and not past either explicit `valid_until` or the new one-time replay TTL configured by `WINDOWS_AGENT_PENDING_MESSAGE_TTL_MINUTES`. The same sync path also stops treating already `Read` jobs as pending messages. Documentation is synchronized in `docs/openapi.yaml`, `docs/technical-implementation-plan.md`, `docs/database-schema-specification.md`, `docs/deployment-and-environment.md`, and `docs/open-questions-and-challenges.md`. Verification passed with `npm run backend:typecheck` and `npm run backend:build`.
- `2026-08-30`: shared-device Windows Agent replay suppression is now tightened further on the server side. A new migration `0015_phase4_agent_session_active_user.up.sql` adds `device_sessions.active_user_identifier`, the active session now refreshes that field from session creation plus later heartbeat or lifecycle reports, and `GET /agent/messages` now suppresses jobs whose latest `Displayed`, `Read`, or `Responded` evidence belongs to a different active user on the same device. Documentation is synchronized in `docs/openapi.yaml`, `docs/technical-implementation-plan.md`, `docs/database-schema-specification.md`, and `docs/open-questions-and-challenges.md`. Verification passed with `npm run backend:migrate:dev`, `npm run backend:typecheck`, and `npm run backend:build`.
- `2026-07-14`: backend and admin authoring now honor release-scope channel guardrails through `ENABLED_DELIVERY_CHANNELS` and `VITE_ENABLED_DELIVERY_CHANNELS`, so out-of-scope channels are hidden in the compose/edit UI and rejected by the backend with `422 CHANNEL_NOT_ENABLED`.
- `2026-07-14`: new operational and security guidance is now documented in `docs/operational-runbook.md` and `docs/security-and-access-model.md`, covering deployment, rollback, incident escalation, device-centric trust assumptions, and production release-scope enforcement for the desktop-first live path.
- `2026-07-14`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, and `node backend/tmp/phase4-release-scope-smoke.mjs`; the smoke started a dedicated runtime on `BACKEND_PORT=4028` with `ENABLED_DELIVERY_CHANNELS=WindowsAgent`, confirmed a desktop-targeted draft could still be created, and confirmed a `WhatsApp` draft request was rejected with `422 CHANNEL_NOT_ENABLED`.
- `2026-07-14`: authenticated operational diagnostics are now available through `GET /health/diagnostics`, returning database reachability, enabled delivery channels, configurable admin and agent session TTL summaries, realtime connection counts, and device connectivity summaries for desktop-first go-live checks.
- `2026-07-14`: baseline device trust revocation is now available through `POST /devices/{deviceId}/revoke-session`, which deletes persisted device sessions, disconnects active realtime streams, marks the device offline, and records a `RevokeDeviceSession` audit entry.
- `2026-07-14`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, and `node backend/tmp/phase4-diagnostics-and-revoke-smoke.mjs`; the smoke started a dedicated runtime on `BACKEND_PORT=4029` with `ADMIN_SESSION_TTL_MINUTES=120`, `AGENT_SESSION_TTL_MINUTES=30`, and `ENABLED_DELIVERY_CHANNELS=WindowsAgent`, confirmed `GET /health/diagnostics` returned the expected database/session/realtime summaries, revoked an active device through `POST /devices/{deviceId}/revoke-session`, and then confirmed the old agent token failed on `POST /agent/heartbeat` with `401 UNAUTHORIZED`.
- `2026-07-14`: admin session rotation is now available through `POST /auth/rotate-session`, immediately invalidating the prior bearer token while issuing a fresh session token for the same authenticated operator.
- `2026-07-14`: directory integration security and environment handling are now hardened for the desktop-first live path: production startup rejects `LDAP_URL=ldap://...` unless `LDAP_ALLOW_INSECURE_URL=true` is set explicitly, production also rejects `LDAP_SKIP_TLS_VERIFY=true`, and structured backend logs now redact common secret-bearing keys such as `password`, `token`, and `authorization`.
- `2026-07-14`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, and `node backend/tmp/phase4-security-hardening-smoke.mjs`; the smoke started a dedicated runtime on `BACKEND_PORT=4030`, confirmed `POST /auth/rotate-session` returned a new bearer token, confirmed the old token failed on `GET /auth/me` with `401 UNAUTHORIZED`, and then confirmed a separate production-mode startup on `BACKEND_PORT=4031` failed fast for `LDAP_URL=ldap://directory.example.internal` without an explicit insecure override.
- `2026-07-14`: backend request tracing now has a lightweight correlation baseline: every HTTP response echoes `X-Request-Id`, and request lifecycle logs include the same `requestId` plus `actorUsername` where available.
- `2026-07-14`: `GET /health/diagnostics` now returns explicit warning and critical `alerts` for expiring sessions, stale realtime connectivity, device staleness, and database reachability so live operators can prioritize degraded conditions without manually interpreting raw counters.
- `2026-07-14`: hybrid recurring reminder authoring and monitoring are now surfaced in the admin flow: create draft supports explicit `Reminder` authoring, publish supports `Recurring` mode with recurrence rule, timezone, execution mode, and validity window, communication detail exposes schedule metadata, and admin monitoring now includes reminder-policy activity plus reconciled reminder events through `GET /communications/{communicationId}/reminder-activity`.
- `2026-07-14`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, and `node backend/tmp/phase4-reminder-hybrid-smoke.mjs`; the smoke started a dedicated runtime on `BACKEND_PORT=4033`, authenticated through LDAP-backed admin login, created a device-targeted reminder draft, published it with `publishMode=Recurring` and `executionMode=AgentLocalRoutine`, confirmed communication detail exposed `scheduleType=Recurring`, and confirmed reminder activity returned at least one materialized reminder policy.
- `2026-07-14`: the admin create and edit flows now also expose explicit `Device` targeting, closing the remaining operator gap for `AgentLocalRoutine` reminders that need a clearly device-bound Windows Agent audience instead of relying on broader audience scopes.
- `2026-07-15`: the publish dialog now exposes visible `AgentLocalRoutine` guardrails and disables publish when a recurring local routine lacks a bounded validity window, lacks eligible Windows Agent recipients, loses the Desktop Agent channel, or uses Emergency or Critical priority. Frontend verification passed again with `npm run build`, and backend type safety remained clean with `npm run backend:typecheck`.
- `2026-07-15`: focused browser validation now also passes on the reminder admin UX using a frontend-real / API-mocked Playwright flow through login, reminder draft creation, device-targeted detail view, recurring publish dialog, `AgentLocalRoutine` guardrail enablement after `Valid Until`, and post-publish `Reminder Schedule` plus `Reminder Activity` visibility. The initial browser blocker was traced to an out-of-date local mock shape for `audience-preview` rather than a production route bug.
- `2026-07-15`: reminder recurrence is now authored earlier in the workflow instead of first appearing at publish time. Create and edit draft now persist first occurrence, recurrence rule, timezone, execution mode, and validity window into draft reminder state, and the publish dialog is prefilled from that stored definition. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, `npm run backend:migrate:dev`, and a real browser flow on `http://127.0.0.1:8080` that logged in, created a reminder draft with recurring authoring data, reopened `Edit Draft` to confirm the values persisted, and confirmed the publish dialog reused the same recurring definition. Evidence screenshots: `tmp/browser-validation-real/21-reminder-authoring-create.png`, `tmp/browser-validation-real/22-reminder-authoring-detail.png`, `tmp/browser-validation-real/23-reminder-authoring-edit.png`, `tmp/browser-validation-real/24-reminder-authoring-publish.png`.
- `2026-07-15`: Notification Center now better matches the operator workflow for one-time and recurring communications. The list view is segmented into `Drafts`, `Scheduled / Live`, and `History`, exposes direct `Edit Draft` entry from the list, and supports lifecycle-aware bulk actions so operators can duplicate selected communications as new drafts and cancel multiple scheduled or active rows together without leaving the list. Focused verification passed with `npm run build` and `npm run backend:typecheck`.
- `2026-07-15`: a draft-to-detail regression in the admin frontend is now fixed. After the Notification Center quick-edit search-param support was introduced, the detail page briefly rendered fewer hooks while draft data was still loading and then rendered more hooks once the query resolved, which caused a React hook-order runtime error right after draft creation. The quick-edit effect now runs before the loading guard so draft submission can navigate safely into detail again. Verification passed with `npm run build`.
- `2026-07-15`: device connectivity testing is now available directly from the `Devices` admin view through `POST /devices/{deviceId}/test-notification`. The backend creates and immediately publishes a one-time device-scoped `OperationalNotice`, the frontend `Test` action now calls that endpoint instead of throwing a placeholder error, and the resulting communication is visible through the normal Notification Center and Windows Agent reconciliation flow. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, and `node backend/tmp/phase4-device-test-notification-smoke.mjs`; the smoke started a dedicated runtime on `BACKEND_PORT=4034`, authenticated an admin, created an agent session for `device-mti-ops-01`, sent a custom `Modal` test notification through `POST /devices/401317fc-fb05-4020-b69f-15c7bd6d90d6/test-notification`, confirmed the response returned `communicationStatus=Queued`, confirmed `GET /communications/{communicationId}` preserved the custom title and body, confirmed `GET /agent/messages` exposed the pending message for the target device, confirmed `GET /audit-logs?module=Devices&search=SendDeviceTestNotification` returned the audit entry, and challenged the missing-device failure path with `404 DEVICE_NOT_FOUND`.
- `2026-07-15`: the device connectivity quick-test flow now also preserves `instruction` rendering semantics. `POST /devices/{deviceId}/test-notification` accepts optional `instruction`, synthesizes a default instruction when omitted, and returns that value in the response so operators can verify the separate instruction block on Windows Agent even from the one-click `Devices` view. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, and a live smoke against `http://10.60.20.126:4019` that confirmed the normal communication flow already returned `instruction` in `/agent/messages`, then identified the remaining gap in the quick-test path and verified the patched endpoint contract locally.
- `2026-07-15`: the communication contract now preserves optional operator `instruction` text end to end. Draft create/update/detail, OpenAPI, database schema, Windows Agent pending-message sync, and local reminder policy sync now carry `instruction` separately from `body`, so admin previews and agent popup rendering no longer diverge. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, and `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-16`: `Wellness Programs` is now tracked as an explicit open expansion item under Phase 4 rather than hidden inside the generic reminder track. The current controlled direction is to keep it inside the broader `Notifications` domain while using a dedicated authoring and monitoring flow, with execution planning captured in `docs/wellness-program-execution-checklist.md` and product/contract baseline captured in `docs/ohih-program-module-blueprint.md`.
- `2026-07-16`: the first admin-web `Wellness Programs` authoring slice is now implemented with a dedicated submenu and dedicated authoring vocabulary on top of the existing reminder backend contract. The admin web carries structured `wellnessProgram` payloads through shared frontend types and service mapping, communication detail surfaces structured wellness metadata, and Windows Agent preview switches to a wellness-specific bright blue or green card when a `wellnessProgram` payload is present. Verification passed with `npm run build`.
- `2026-07-16`: the admin web now also exposes the first dedicated `Wellness Programs` navigation and list slice. Sidebar navigation includes a `Wellness Programs` entry under the notifications cluster, the new page lists only reminder communications that carry a `wellnessProgram` payload, and the page surfaces recurrence summary, theme, program type, current status, last-updated metadata, and lifecycle-aware quick actions for `view`, `edit`, `publish`, `duplicate`, and `deactivate`. Verification passed with `npm run build`.
- `2026-07-16`: the admin web now also exposes the first wellness monitoring slice without adding new backend endpoints. The dedicated `Wellness Programs` page derives monitoring from `GET /communications/{communicationId}/reminder-activity`, surfacing aggregate event counts, active-policy coverage, last sync, last activity, and a simple compliance percentage, while communication detail now adds wellness-specific reminder summary cards, device-level last activity visibility, and a recent-event timeline for troubleshooting. Verification passed with `npm run build`.
- `2026-07-16`: the Windows Agent now exposes the first dedicated `Eye Break Reminder` rendering slice for `Wellness Programs`. Reminder-policy sync and local SQLite persistence now retain `wellnessProgram`, the client renders `SimpleReminder + Blue` reminders through a dedicated white wellness surface instead of the dark operational notification shell, and the first three eye-break template variants now exist as `ReminderCard`, `CountdownCard`, and `CompletionCard`. Structured wellness CTA actions are also wired to specialized reminder event reporting such as `Completed` and `Snoozed`. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj`.
- `2026-07-17`: the dedicated eye-break renderer is now refined beyond the first slice baseline. `ReminderCard`, `CountdownCard`, and `CompletionCard` now consume authored `title`, `body`, and `instruction` text from the synced wellness payload, countdown-oriented layouts now run a local countdown state inside `WellnessReminderWindow`, and the surface now exposes inline interaction status plus inline error feedback while staying visually separate from the operational notification shell. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj`.
- `2026-07-17`: the eye-break wellness surface is now also repositioned to a smaller toast-like card anchored near the bottom-right work area instead of a centered reminder window, aligning the client behavior more closely with the intended low-friction reminder notice while preserving the dedicated wellness styling. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-17`: the Windows Agent eye-break renderer now also supports an English `OverviewCard` (`A4`) variant, including dedicated view-model state, wellness-window layout, local shell preview trigger, and compact toast sizing aligned with the existing eye-break card family. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`, `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`, and clean diagnostics on the edited `WellnessReminderWindow`, `MainWindow`, and `DebugScenarioService` files.
- `2026-07-17`: the wellness authoring/admin/backend contract now also recognizes `OverviewCard` (`A4`) end to end for `SimpleReminder`. Frontend types, layout selection helpers, admin preview, backend draft validation, and `docs/openapi.yaml` now share the same layout vocabulary, and backend validation now explicitly requires `countdownSeconds` for `CountdownCard` wellness payloads. Verification passed with `npm run backend:typecheck` and `npm run build`.
- `2026-07-17`: `RemindMeLater` on wellness eye-break reminders now performs a true local snooze and re-trigger in the Windows Agent instead of reporting `Snoozed` only. Agent persistence now stores `snoozed_until_utc`, the reminder schedule evaluator prioritizes that local snooze timestamp before normal recurrence, and the slice is covered by focused `ReminderScheduleEvaluatorTests`. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore --filter ReminderScheduleEvaluatorTests`.
- `2026-08-17`: the first `Office Stretching` client slice now exists as `B1` on the Windows Agent wellness surface. The previous debug stretching preview no longer falls back to the generic reminder shell; it now renders through `GuidedRoutine + Green` with a dedicated compact card layout, ordered stretching steps, and `Start Stretching` plus `Remind me in 10 min` actions so office-stretching visual iteration can follow the same local preview loop used for eye-break `A1-A4`. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-08-17`: the admin wellness authoring flow now also carries a more realistic first `Office Stretching` (`B1`) preview baseline. Guided-routine drafts start from stretching-oriented default steps instead of generic placeholders, and the dedicated `Desktop Preview` now mirrors the compact green office-stretching card direction so operators can iterate on copy and CTA labels before the full guided runtime flow is expanded. Verification passed with `npm run build`.
- `2026-08-17`: local development hardening now aligns the Windows Agent with the co-hosted backend runtime and reduces visible clipping in the first office-stretching `B1` slice. The debug client appsettings now target `http://127.0.0.1:4019`, the support-shell `Wellness Preview` description now wraps instead of truncating, and the guided-routine card gets additional vertical space plus top-aligned step content so the stretching prompt no longer collides with the checklist area during local preview. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Debug --no-restore`, `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`, and a live agent restart that successfully re-established `/agent/session`, SSE negotiate, pending-message sync, and reminder-policy sync against `http://127.0.0.1:4019`.
- `2026-08-17`: the first stretching card has now been compressed back toward the eye-break card family footprint after local visual review. `B1` now uses the same `308x325` toast size as the `A` variants, guided-routine inline status text is hidden to reduce vertical drift, and the stretching artwork, checklist spacing, and callout block were tightened so the office-stretching card reads more like a sibling of `A1-A4` instead of a longer custom panel. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Debug --no-restore`, `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`, and a live agent restart from the refreshed debug build.
- `2026-08-17`: the first office-stretching card now follows the same rounded hero-asset pattern used by the approved eye-break surfaces. The temporary vector illustration in `B1` was replaced with a generated PNG resource (`Assets/Wellness/office-stretch-b1.png`) selected to stay closer to the original visual direction, so the stretching card can iterate through art swaps without rewriting XAML geometry. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Debug --no-restore`, `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`, and a live agent restart from the refreshed debug build.
- `2026-08-17`: the approved `B1` office-stretching art was tightened further by promoting the manually cropped panel asset into the official resource path. `Assets/Wellness/office-stretch-b1.png` now points at the true-cropped variant so the rounded hero frame uses the denser composition chosen during visual review instead of the earlier looser draft. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Debug --no-restore`, `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`, and a live agent restart from the refreshed debug build.
- `2026-08-17`: the first office-stretching card was refined again against the approved sample to close the remaining visual gap. The `B1` hero asset now uses a more polished regenerated illustration with tighter subject framing, the top-left stretching icon was redrawn to better match the sample pose, the title block was lifted higher, and the left illustration slot plus supporting text spacing were rebalanced so the card reads closer to the target composition. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Debug --no-restore`, `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`, and a live agent restart from the refreshed debug build.
- `2026-08-17`: the stretching `B1` card was refined a second time specifically to use more of the top header area and remove the boxed-asset feel that still differed from the approved sample. The guided-routine block now shifts upward more aggressively, the top-left icon is enlarged and redrawn again, the left illustration is rendered as a free-flow image without the previous bordered frame, and the green-theme palette now drives the surface, border, text, and primary-button gradient instead of leaving blue-biased defaults in place. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Debug --no-restore`, `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`, and a live agent restart from the refreshed debug build.
- `2026-08-17`: the top-left stretching badge no longer relies on hand-drawn XAML strokes. A dedicated MCP-generated icon asset (`Assets/Wellness/stretching-icon-b1.png`) now drives the badge, while the guided-routine header and first content row were tightened again so the title block, hero art, and checklist align more closely with the approved sample composition. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Debug --no-restore`, `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`, and a live agent restart from the refreshed debug build.
- `2026-08-17`: the next office-stretching client slice now also includes a `B2` overview preview on the Windows Agent wellness surface. The debug shell exposes a dedicated `B2` trigger, stretching `OverviewCard + Green` now renders a compact six-tile step summary instead of reusing the eye-break `A4` metrics layout, and the new `B2` art set is promoted into official agent resources from a single reference-generated master so the six poses stay visually consistent. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-08-21`: the `B2` overview surface now uses a single approved composite resource chosen from the generated set instead of stitching together multiple intermediate variants. The official `office-stretch-b2-content.png` asset was replaced with the selected collage and the stretching overview window dimensions were widened to better match the landscape aspect ratio. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-08-21`: the stretching `GuidedRoutine` shell was upgraded from a compact checklist into a dedicated step-detail template that matches the approved office-stretching direction more closely. The Windows Agent now supports a green header, per-step title and `1 / 6` style progress label, a large step hero area, a circular countdown ring, centered step instruction copy, and local `Kembali / Jeda / Berikutnya` controls. In local preview, clicking `Mulai` from the `B2` overview now opens the first guided-routine step in the new shell. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-08-24`: the stretching flow now also supports dedicated post-routine completion templates. Two green completion variants were added for office stretching (`B4` confirmation and `B5` follow-up reminder), and the current test routing now opens `B4` automatically after the sixth guided-routine step finishes. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-08-24`: the office-stretching green palette was tightened to match the approved sample tone more closely across the whole stretching set (`B1` through the latest completion templates). The runtime green theme now uses a darker, more neutral header/accent pair, and the stretching-specific sub-panels, badges, countdown track, and header subtext were moved onto shared theme brushes so the same green family applies consistently across overview, guided routine, and completion states. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-08-24`: the stretching guided-routine countdown was refined from a dotted ring into a continuous circular progress arc with rounded end caps, closer to the approved neck-stretch sample. The numeric countdown label remains centered while the surrounding ring now fills as the step progresses. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-08-24`: the stretching guided-routine center control now uses an icon-only play/pause button instead of a text label, preserving the same local pause behavior while matching the approved mockup direction more closely. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-08-24`: the stretching guided-routine timing behavior was tightened for operator review. The center play/pause control was reduced so the surrounding navigation buttons keep more horizontal room, and the per-step countdown now stops at zero instead of auto-advancing to the next exercise. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-08-25`: guided-routine step navigation now fully resets the active countdown for each exercise. When an operator uses `Kembali` or `Berikutnya`, the new step starts with a fresh timer instead of inheriting the stopped countdown state from the previous step. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-07-17`: reminder activity reconciliation now tolerates temporary offline windows in the Windows Agent. Retryable reminder events are persisted into a local SQLite queue, `AgentConnectionWorker` flushes that queue after the session and sync loop recover, and focused tests now cover both queue persistence and service-level flush behavior. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore --filter "AgentStateStoreTests|ReminderEventServiceTests"`.
- `2026-07-17`: the admin IA is now aligned with the locked product boundary that separates standard notifications from wellness programs. `Notification Center` now excludes wellness items, `Create Notification` no longer exposes wellness toggles, and `Wellness Programs` now owns a dedicated `Create Wellness Program` entry while still reusing the existing reminder-oriented backend APIs. Verification passed with `npm run build`.
- `2026-07-18`: the remaining admin drift in wellness draft editing is now removed. Wellness list edit actions now open the dedicated `Wellness Programs` editor, and the shared notification detail page no longer embeds a `Wellness Program Mode` inside the generic draft form. Verification passed with `npm run build`.
- `2026-07-18`: the remaining wellness view/publish drift is now removed from the admin UX. Wellness list row navigation plus `View` and `Publish` actions now open a dedicated `Wellness Programs` detail route with wellness-focused publish guardrails and monitoring, instead of routing operators back through the generic notification detail page. Verification passed with `npm run build`.
- `2026-07-15`: notification authoring now exposes explicit `Windows Agent presentation` selection in both create and draft-edit flows whenever the desktop channel is enabled. Frontend state now persists `Toast`, `Modal`, or `Fullscreen` directly into the communication payload instead of inferring `Modal` from priority, and the detail view now surfaces the stored presentation value. Verification passed with `npm run build`.
- `2026-07-15`: Windows Agent authoring semantics are now enforced consistently across admin UI, backend draft validation, and client rendering. `Info + Toast` clears the separate `instruction`, `Warning` is forced to `Modal` with required `instruction`, and the Windows Agent toast surface now auto-dismisses without interactive action buttons so it behaves like a real transient toast. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, and `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: backend and Windows Agent contracts now support optional per-notification `toastAutoDismissSeconds` overrides for Windows Agent toast rendering. The value is persisted on `communications` and `agent_reminder_policies`, synced through `/agent/messages` and reminder policy APIs, and rendered by the agent with a 5-second fallback when omitted. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, and `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: Notification Center authoring now exposes `Toast Auto Dismiss Seconds` whenever the desktop channel resolves to `Toast`, the detail view shows the stored override versus the 5-second default, and migration `0012_phase4_toast_dismiss_duration.up.sql` has been applied manually on the active development database because older checksum mismatches still block the normal migration runner. Verification passed with `npm run build` and `npm run backend:migrate:status:dev` showing `0012` as `applied`.
- `2026-07-15`: Windows Agent warning-tone rendering now respects the actual communication priority for server messages instead of only distinguishing `Critical` versus `Reminder`. Warning communications now show the correct warning badge/theme while reminders keep their existing warning treatment. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: Windows Agent toast delivery now uses a hybrid path. `Toast` notifications without `toastAutoDismissSeconds` can render through native Windows toast surfaces, while notifications that require exact custom timing continue using the existing WPF toast surface. This keeps the new server-side timing override authoritative without giving up native Windows toast behavior for default-duration info toasts. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj`.
- `2026-07-15`: local validation of the hybrid toast path exposed that `run-agent.ps1` was still launching the legacy `net8.0-windows` output folder after the client target framework moved to `net8.0-windows10.0.17763.0`. The helper script now launches the correct build output so runtime checks exercise the native-toast-capable binary. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: Windows Agent modal presentation is now constrained to a tighter maximum footprint, the primary message body is truncated for faster scanning, and long-form markdown instruction content scrolls inside the instruction panel instead of forcing the whole modal surface to grow. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: Communication authoring now enforces short-form operational message bodies with a 320-character limit in admin create/edit flows and backend validation, while the Windows Agent modal footer keeps action buttons visible outside the instruction content region so long markdown guidance no longer pushes the controls out of view. Verification passed with `npm run backend:typecheck`, `npm run build`, and `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: duplicate-to-draft editing now supports priority changes end to end. The admin `Edit Draft` dialog exposes `Priority`, `PATCH /communications/{communicationId}` now accepts draft priority updates, and Windows Agent authoring guardrails are recalculated from the edited priority so `Warning` still forces `Modal` plus instruction while `Info + Toast` still clears instruction. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, and `npm run build`.
- `2026-07-15`: the Windows Agent modal footer now preserves the same rounded lower corners as the main modal shell, so the fixed action area no longer looks visually squared off after the footer-layout hardening change. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: Notification Center delivery visibility now uses device-centric recipient naming for Windows Agent device deliveries, preferring hostname or device identifier over inherited employee names so delivery logs no longer misleadingly show employee placeholders such as shared-device owners. The `Responses` empty state also now distinguishes between "no responses yet" and communications that never used a workflow response. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, and `npm run build`.
- `2026-07-15`: the Windows Agent tray shell now uses a custom branded popup instead of the default WPF context menu. The tray surface now shows `MTI Alert`, live connection status, and the requested command set for `Open Dashboard`, `Notification History`, `Settings`, `About`, and `Exit`, while left-click still brings the dashboard forward for quick support access. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: the Windows Agent reminder-policy reader now tolerates legacy or partially populated local rows where `requires_response` is null, preventing the local reminder scheduler from repeatedly throwing `The data is NULL at ordinal 12` after startup. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: the Windows Agent reminder scheduler no longer misreads local reminder-policy rows after the reminder contract expansion. `GetActiveReminderPoliciesAsync()` now selects `toast_auto_dismiss_seconds` in the same ordinal order expected by `MapReminderPolicyState()`, malformed local `workflow_json` now degrades to `null` instead of crashing the scheduler, and stale local reminder policies are automatically deactivated if the server rejects reminder event reporting with `404 policy not found`. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: the Windows Agent connection state handling no longer marks the shell `Degraded` during clean startup just because the realtime client performs a pre-connect disconnect. The realtime client now emits `Disconnected` only when an actual prior connection attempt existed, the connection worker only promotes `Disconnected` to `Degraded` after a previously established `Connected` state, and tray initialization no longer overwrites live runtime connection text with a static startup placeholder. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: tray interaction was hardened after the first branded popup regression. Right-click now uses a stable native tray `ContextMenu` that always exposes `Open Dashboard`, `Notification History`, `Settings`, `About`, and `Exit`, while connection-state transitions now surface `Reconnecting` for transient realtime loss instead of prematurely collapsing the shell to `Degraded` without a confirmed connection-cycle failure. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-07-15`: agent startup no longer stalls before session acquisition. The root cause was tray property-change handlers mutating WPF `TaskbarIcon` and tray menu items from the background connection worker thread when `RuntimeStateService.ConnectionStatus` changed to `Connecting`. Tray UI updates are now marshalled onto the WPF dispatcher, and release verification confirmed the full startup path now proceeds through cached session loading, session refresh, realtime SSE connection, heartbeat, message sync, and reminder-policy sync. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore` plus live log validation from `agent-20260715.log`.
- `2026-07-15`: after the tray thread-safety fix was verified, the branded custom tray popup was restored for right-click interactions. The tray still keeps left-click quick-open behavior for the dashboard, but right-click once again shows the MTI Alert popup menu with live connection status and the requested command set, now backed by dispatcher-safe runtime-state updates. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore`.
- `2026-07-15`: the Windows Agent support shell now follows a leaner dark Fluent direction instead of the earlier diagnostics dashboard layout. The main WPF window remains a single MVVM shell, but now uses shared theme resources, rounded dark surfaces, custom header chrome, concise runtime overview rows, a small recent-notification summary sourced from local agent state, and a collapsed developer-tools section so the app feels like a background utility rather than an admin console. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore`.
- `2026-07-15`: the support shell was tightened again after visual review so it now targets a fixed single-screen portrait composition rather than a scrollable support panel. The shell removes the main-window debug section, trims runtime rows to the five operator-relevant facts, compacts recent-notification items, reduces footer actions to `Settings` and `About`, and constrains the shell window to a fixed design size so the full surface fits in one view like the reference composition. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore`.
- `2026-07-15`: tray popup interaction was hardened again after a right-click hang regression. Tray left-click and right-click handlers now always marshal menu show/hide and shell-open actions onto the WPF dispatcher, and tray-menu status refresh now uses asynchronous dispatcher enqueueing instead of synchronous invoke so custom tray popup activation does not block when NotifyIcon raises events off the UI thread. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore`.
- `2026-07-15`: the Windows Agent shell navigation is now split cleanly across dedicated operator surfaces. `Open Dashboard` now brings forward the main dark Fluent shell, `Notification History` and `View All` both open a same-size local history window sourced from persisted agent message state, and `About` now opens as a separate dark modal with agent identity and runtime summary details. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore`.
- `2026-07-15`: the custom Windows Agent shell windows now support more reliable drag behavior from their header chrome. The main shell, notification history window, and about dialog now use preview mouse-down handling for the title bar and explicitly skip header-button clicks, which prevents nested header content from swallowing the move gesture before `DragMove()` executes. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore`.
- `2026-07-15`: the primary Windows Agent support shell now also supports drag from any non-interactive surface instead of relying only on the title strip. The main window listens at the window level for preview mouse-down, skips interactive controls such as buttons and scrollbars, and then starts `DragMove()` so the shell feels movable even when the user grabs normal content areas. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore /p:OutputPath=.\\MTI.Alert.Agent\\artifacts\\verify-main-window-drag\\`.
- `2026-07-15`: the notification history shell now mirrors the same drag behavior as the main support shell, so the fixed-size history window can be moved by dragging any non-interactive surface instead of only the narrow title strip. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore /p:OutputPath=.\\MTI.Alert.Agent\\artifacts\\verify-stability-fixes\\`.
- `2026-07-15`: tray right-click handling was simplified again in favor of runtime stability. The extra custom popup window was replaced by a native WPF `ContextMenu` on the tray icon, keeping the same operator actions while removing the separate top-level popup surface that could freeze the agent UI during focus and activation churn. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore /p:OutputPath=.\\MTI.Alert.Agent\\artifacts\\verify-stability-fixes\\`.
- `2026-07-15`: the stabilized native tray context menu now also carries the approved branded light-card styling instead of falling back to a plain default menu. Resource-based templates now provide the rounded white surface, connection-status header, separators, item glyphs, hover affordances, and red exit action while keeping the safer native context-menu behavior. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore /p:OutputPath=.\\MTI.Alert.Agent\\artifacts\\verify-tray-theme\\`.
- `2026-07-14`: focused verification passed with `npm run backend:typecheck`, `npm run backend:build`, and `node backend/tmp/phase4-observability-smoke.mjs`; the smoke started a dedicated runtime on `BACKEND_PORT=4032` with `ADMIN_SESSION_TTL_MINUTES=10` and `AGENT_SESSION_TTL_MINUTES=10`, confirmed `GET /health/diagnostics` echoed `X-Request-Id=phase4-observability-request`, returned `ADMIN_SESSIONS_EXPIRING_SOON` plus `AGENT_SESSIONS_EXPIRING_SOON` alerts, and wrote a matching `http.request.completed` log line with the same request ID for correlation.
- `2026-07-14`: a desktop-first Docker deployment baseline now exists through `Dockerfile.backend`, `Dockerfile.frontend`, `docker-compose.yml`, `docker-compose.with-postgres.yml`, `.dockerignore`, and `.env.docker.example`, giving the project a reproducible local container path for frontend and backend by default, with PostgreSQL available as an explicit optional overlay.
- `2026-07-14`: focused Docker-baseline verification passed with `npm run backend:build`, `NITRO_PRESET=node-server npm run build`, and a direct runtime check of `node .output/server/index.mjs`; the frontend Node SSR output listened successfully on `127.0.0.1:4090`, confirming the Docker-targeted `node-server` preset is viable for the current TanStack Start stack.
- `2026-07-14`: the backend Docker image now also includes `backend/migrations` in addition to `backend/dist`, fixing container startup for `node backend/dist/scripts/run-migrations.js up` when the service boots under Compose.
- `2026-07-14`: the Docker publish path now includes an admin reverse-proxy gateway so frontend assets can use same-origin `/api` routing instead of embedding a raw backend host, which removes the observed mixed-content and CORS failure path during HTTPS login from `https://mtialert.merdekabattery.com`.
- `2026-07-14`: reminder hybrid behavior is now called out as an explicit remaining Phase 4 UX gap: backend reminder schedules, policy sync, and reminder-event reconciliation are implemented, but admin authoring and monitoring still need dedicated reminder UX surfaces for recurrence controls, execution-mode clarity, and policy/evidence visibility. The supporting source-of-truth reference is `docs/reminder-hybrid-ux.md`.
- `2026-07-14`: source-of-truth reminder UX expectations are now synchronized in `docs/functional-specification.md`, `docs/go-live-checklist.md`, `docs/open-questions-and-challenges.md`, and `docs/reminder-hybrid-ux.md`, so hybrid reminder authoring and monitoring can no longer be treated as implicitly complete based only on backend contract readiness.
- `2026-07-15`: a release-readiness `Software Acceptance Test` baseline is now documented in `docs/software-acceptance-test.md`, aligning desktop-first manual acceptance coverage across admin authentication, authoring guardrails, Windows Agent connectivity, reminder policy sync, monitoring evidence, and security-sensitive operations such as session rotation and device-session revocation.
- `2026-08-25`: the office-stretching guided-routine now uses the approved human-photo asset `Assets/Wellness/exec-df3dbf67-b065-459a-9515-4d9420dcc2d9.png` for the first `neck` step in both the debug preview scenario and the shared fallback resolver, so step 1 stays aligned with the chosen runtime art even if a step payload omits `AssetUrl`. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`, asset file-existence verification, and source inspection confirming the updated `pack://application:,,,/Assets/Wellness/exec-df3dbf67-b065-459a-9515-4d9420dcc2d9.png` mapping in both `DebugScenarioService` and `NotificationWindowViewModel`.
- `2026-08-25`: the office-stretching guided-routine now also uses the approved shoulders resource `Assets/Wellness/2026-08-18T07-58-31-515Z-use-the-reference-image-as-the-exact-same-person-in-the-same-1.png` for step 2 in both the debug preview scenario and the shared fallback resolver, keeping the second movement aligned with the selected runtime photo during local preview and fallback rendering. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`, asset file-existence verification, and source inspection confirming the updated `pack://application:,,,/Assets/Wellness/2026-08-18T07-58-31-515Z-use-the-reference-image-as-the-exact-same-person-in-the-same-1.png` mapping in both `DebugScenarioService` and `NotificationWindowViewModel`.
- `2026-08-25`: the office-stretching guided-routine step 3 now uses the updated upper-back resource `Assets/Wellness/exec-5993a3a0-df9f-4793-ae02-d37e39efc18b.png` in both the debug preview scenario and the shared fallback resolver, replacing the previous step-3 photo selection so the third movement follows the latest approved runtime art during local preview and fallback rendering. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`, asset file-existence verification, and source inspection confirming the updated `pack://application:,,,/Assets/Wellness/exec-5993a3a0-df9f-4793-ae02-d37e39efc18b.png` mapping in both `DebugScenarioService` and `NotificationWindowViewModel`.
- `2026-08-25`: the office-stretching guided-routine now also uses the approved lower-back resource `Assets/Wellness/exec-7caa467e-b008-42c3-b732-a4341424e295.png` for step 4 in both the debug preview scenario and the shared fallback resolver, keeping the fourth movement aligned with the selected runtime photo during local preview and fallback rendering. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`, asset file-existence verification, and source inspection confirming the updated `pack://application:,,,/Assets/Wellness/exec-7caa467e-b008-42c3-b732-a4341424e295.png` mapping in both `DebugScenarioService` and `NotificationWindowViewModel`.
- `2026-08-25`: the office-stretching guided-routine now also uses the approved wrist resource `Assets/Wellness/exec-c043831b-a005-4a11-87e6-56e0748784f8.png` for step 5 in both the debug preview scenario and the shared fallback resolver, keeping the fifth movement aligned with the selected runtime photo during local preview and fallback rendering. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`, asset file-existence verification, and source inspection confirming the updated `pack://application:,,,/Assets/Wellness/exec-c043831b-a005-4a11-87e6-56e0748784f8.png` mapping in both `DebugScenarioService` and `NotificationWindowViewModel`.
- `2026-08-25`: the office-stretching guided-routine now also uses the approved stand-and-move resource `Assets/Wellness/exec-a3a7ce7a-491d-4381-9af8-2964409fb2ba.png` for step 6 in both the debug preview scenario and the shared fallback resolver, keeping the sixth movement aligned with the selected runtime photo during local preview and fallback rendering. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`, asset file-existence verification, and source inspection confirming the updated `pack://application:,,,/Assets/Wellness/exec-a3a7ce7a-491d-4381-9af8-2964409fb2ba.png` mapping in both `DebugScenarioService` and `NotificationWindowViewModel`.
- `2026-08-25`: the active office-stretching guided-routine photo assets for steps 1 through 6 were refined to use transparent backgrounds instead of the original flat gray studio backdrop, so the hero images can sit more cleanly inside the white stretching card without the rectangular photo-box look. Verification passed with alpha-channel inspection confirming `(0, 255)` transparency ranges on all six active PNG resources and `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-08-25`: after visual review rejected the first local cutout attempt, the active office-stretching step 1 through 6 assets were replaced with OpenAI `gpt-image-2` transparent-background edits generated from the exact approved pose references. The existing asset file paths were preserved, previous versions were backed up under `Assets/Wellness/backup-openai-transparent-20260825/`, and verification passed with alpha-channel inspection confirming `(0, 254)` transparency ranges on the promoted PNGs plus `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore`.
- `2026-08-30`: the current `Wellness Programs` boundary is now tightened further in the source-of-truth docs after clarifying CTA semantics and monitoring expectations. `docs/functional-specification.md`, `docs/technical-implementation-plan.md`, `docs/ohih-program-module-blueprint.md`, `docs/wellness-program-execution-checklist.md`, `docs/open-questions-and-challenges.md`, and `MTI.Alert.Agent/docs/client/functional-specification.md` now lock that wellness assignment remains device-centric, `GotIt` and `Done` count as performed or completed confirmation, `RemindMeLater` counts as defer or snooze rather than skip, and active-user identity remains optional audit metadata rather than the primary target key. The same amendment also records that the dedicated post-routine usefulness or need feedback prompt is still not implemented and remains an explicit post-MVP expansion item. Verification passed through cross-document review confirming the new wording is consistent with the existing reminder-policy sync, admin monitoring, and Windows Agent wellness rendering slices already recorded in this roadmap.
- `2026-08-30`: the first wellness end-to-end runtime hardening pass is now in progress under `docs/checkpoints.md` checkpoint `wellness_e2e_server_to_client`. Three concrete sync defects were corrected in source: `backend/src/modules/agent/service/agent-service.ts` now accepts `OverviewCard` in the agent reminder-policy parser, reminder-policy sync now returns `isActive` plus `updatedAt` so inactive replacement rows can deactivate local policies, and the Windows Agent reminder-policy sync now advances its cursor from the latest policy `updatedAt` instead of re-saving the stale cursor. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`, and `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore` (tests passed before sandbox cleanup restrictions on temp directories).
- `2026-08-30`: the next checkpoint validation now proves the patched wellness path on a real device runtime. Using local backend runtime `http://127.0.0.1:4019`, `Installer\\invoke-agent-rollout.ps1` signed the rebuilt `1.0.7` MSI, published it to `GET /agent/packages/local/MTI.Alert.Agent.Setup.msi`, and created rollout intent `35c89d83-d7aa-4d77-bb13-b857956c2488` for device `MTI-NB-373`. A dedicated `OverviewCard` wellness communication (`c4a88b28-a328-479b-9578-d0770cdf3334`) was then created and published against the same backend, materializing reminder policy `0d590d3c-1a78-4fe1-9e3d-8022f9ce915b` for `MTI-NB-373` with `layoutVariant = OverviewCard`, `lastSyncedAt = 2026-08-30 02:59:42.470696+00`, and later real device reminder events including `Displayed`. This confirms the live server publish -> reminder-policy sync -> Windows Agent render path after the `OverviewCard` plus cursor plus inactive-row sync fixes. Remaining runtime gap: the device still reported `agentVersion = 1.0.6` during this checkpoint, so rollout publication for `1.0.7` is verified but rollout completion is not yet evidenced.
- `2026-08-30`: the same runtime validation uncovered a client interaction bug on the live `OverviewCard` popup. `MTI.Alert.Agent/Presentation/Notifications/WellnessReminderWindow.xaml.cs` previously waited for reminder event reporting to finish before allowing the window to close, so transient event-report latency could make the close button plus wellness CTA buttons appear unclickable even though the handler had started. The window now closes optimistically first and lets `Dismissed`, `Completed`, or `Snoozed` reporting continue in the background with warning logs on failure. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-08-30`: the dedicated `Wellness Programs` frontend monitoring surfaces were hardened after operator review showed the page could mislead with an empty `0` state while backend-backed wellness data already existed. `src/routes/_app.wellness-programs.tsx` now surfaces query failure explicitly, adds manual refresh, and polls every 15 seconds so externally published wellness changes become visible without a page reload. `src/routes/_app.wellness-programs.$id.tsx` now applies the same 15-second refresh baseline to the detail, audience, delivery, and reminder-activity panels, plus a manual refresh action for operator review. Verification passed with `npm run build`.
- `2026-08-30`: a second frontend runtime fix closed the remaining dedicated wellness page blocker. `src/services/notifications.service.ts` had been relying on `this.list()`, `this.get()`, and similar calls inside exported object methods, but TanStack Query invokes `notificationsService.listWellnessPrograms` as an unbound callback, so `this` became `undefined` and the page failed with `Cannot read properties of undefined (reading 'list')`. The affected methods now call `notificationsService.*` explicitly, which restores list loading, monitoring refresh, and the route path behind `Create Wellness Program`. Verification passed with `npm run build`.
- `2026-08-30`: wellness authoring is now being narrowed from freeform operator-written copy to a locked template catalog so the Windows Agent surface can be tested without layout drift. `src/lib/wellness-template-catalog.ts` now defines the supported admin-facing templates `A1`, `A2`, `A4`, `B1`, and `B2`, `src/routes/_app.wellness-programs.new.tsx` now drives creation and editing from those templates instead of editable `Program Title` / `Program Summary` / `Instruction` fields, and `src/components/notifications/DesktopPreview.tsx` now distinguishes the stretching `B2` overview preview from the eye-break overview card. On the backend, `backend/src/modules/communications/service/communication-draft-service.ts` now allows `GuidedRoutine + OverviewCard`, which unlocks the real `B2` publish path instead of leaving it as an agent-only debug slice. Verification passed with `npm run build` and `npm run backend:build`. Runtime browser confirmation against the live admin origin still requires a frontend runtime rebuild because the temporary local dev origin hit the existing `VITE_API_URL=http://127.0.0.1:4019` cross-origin login restriction.
- `2026-08-31`: the stretching template semantics were corrected again after the latest operator visual review. The publishable server-side entry point is now `B2`, not `B1`: `B2` is the office-stretching start card (`GuidedRoutine + Green + OverviewCard`) with the compact overview shell, `Office Stretching`, `2 - 3 Menit`, the checklist summary, and `Mulai` / `Ingatkan 10 Menit Lagi` style CTA semantics, while the guided per-step exercise shell is treated as the internal `B1` follow-up state that only appears after the user chooses start. `src/lib/wellness-template-catalog.ts` now exposes only the publishable `B2` entry card in admin authoring, and `MTI.Alert.Agent/Services/DebugScenarioService.cs` now maps the local debug flow to the same `B2 -> B1` runtime direction so preview naming no longer teaches the wrong template order. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore` and `npm run build`.
- `2026-08-31`: the local `wellness_e2e_server_to_client` checkpoint now also proves the publishable `B2` path on the live target device after the latest template remap. On backend `http://127.0.0.1:4019`, admin-authenticated runtime calls created and published recurring `B2` draft `3e8cbad5-d553-4d85-900b-201b606acfdf` for device `MTI-NB-373`, materializing reminder policy `0ae96c33-39ca-4e4a-86d5-9b66b00f4c72` with `GuidedRoutine + Green + OverviewCard`; the same target device now reports `agentVersion = 1.0.7` and `status = Online`. A second minutely verification draft `6e07c523-887a-4a3f-bd39-929905611a55` then proved live schedule execution: reminder policy `fba67fe7-3755-4556-87ae-b2841bc43714` synced with `lastSyncedAt = 2026-08-31 04:24:55.179574+00`, and reminder activity recorded real `Triggered` plus `Displayed` events for the same `B2` occurrence on `MTI-NB-373`. No `Started` event was recorded during that verification window, which is consistent with the intended `B2 -> B1` runtime where the guided exercise countdown does not auto-start until the user chooses `Start`. Remaining gap: the exact agent `B2` visual shell still needs to be re-aligned with the latest screenshot-driven collage layout even though the server publish -> sync -> render flow is now evidenced.
- `2026-08-31`: after the latest operator clarification, the office-stretching runtime mapping is now split into two visible start-card variants instead of treating `B1` as an internal-only guided state. On the Windows Agent, `MTI.Alert.Agent/Presentation/Notifications/WellnessReminderWindow.xaml` now renders `B1` as the hero-plus-checklist start card and `B2` as the compact collage-grid start card, while `MTI.Alert.Agent/ViewModels/NotificationWindowViewModel.cs` now exposes dedicated overview variant flags so both templates can share the same `OverviewCard` transport shape without collapsing into one layout. `MTI.Alert.Agent/Presentation/Notifications/WellnessReminderWindow.xaml.cs` also now opens the same guided routine transition when either start-card primary action is pressed, so the step-by-step countdown remains a post-`Start` flow instead of appearing immediately. Local preview/debug routing was updated in `MTI.Alert.Agent/Services/DebugScenarioService.cs` and `MTI.Alert.Agent/MainWindow.xaml` so `B1` and `B2` can both be reviewed explicitly. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`.
- `2026-09-03`: the dedicated `Wellness Programs` authoring and publish UX is now aligned more closely with operator review. `src/lib/wellness-template-catalog.ts` now re-exposes `A3` and both visible stretching start cards `B1` plus `B2`, `src/routes/_app.wellness-programs.new.tsx` now replaces raw RRULE entry with an interval-and-unit cadence builder, adds `Never Expires` support, exposes `rotationMode` as `Fixed`, `Sequential`, or `Shuffle`, and allows bulk device selection for one draft, while `src/routes/_app.wellness-programs.$id.tsx` now uses the same operator-friendly schedule language during detail review and publish. Frontend notification mapping now preserves multiple device targets through `targetDeviceIds` so the same draft can materialize multiple device-bound reminder targets without changing the underlying device-centric contract. Supporting source-of-truth wording is synchronized in `docs/functional-specification.md` and `docs/wellness-program-execution-checklist.md`. Verification passed with `npm run build`.
- `2026-09-03`: the wellness refinement pass now carries the operator concept all the way through runtime. `src/routes/_app.wellness-programs.new.tsx` now starts from `Program Family` (`20-20-20 Rule` vs `Office Stretching`) instead of a flat template dropdown, enables multi-variant selection per family, and only exposes `rotationMode` as a meaningful control when multiple variants are chosen. `src/components/wellness/WellnessScheduleFields.tsx` now also exposes `Distribution Mode` (`Synchronized` vs `Staggered`) plus a stagger window so bulk wellness rollout does not have to fire simultaneously. Backend reminder scheduling now persists distribution metadata through `publish_request_json` and materializes staggered `agent_reminder_policies` with deterministic per-device `valid_from` offsets inside `backend/src/modules/communications/service/communication-draft-service.ts`, while the agent sync/parser plus `MTI.Alert.Agent/Services/NotificationRenderer.cs` now carry `variantKeys` and resolve the effective wellness variant per occurrence using `Fixed`, `Sequential`, or `Random` rotation at render time. Verification passed with `npm run build`, `npm run backend:typecheck`, `npm run backend:build`, and `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj --no-restore`. `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore` passed all 27 tests, but the sandbox reported restricted cleanup paths after completion.
- `2026-09-04`: the wellness detail monitoring now exposes effective execution timing per device without requiring a new agent package. `src/routes/_app.wellness-programs.$id.tsx` now derives `Next Run` and schedule state from the existing reminder policy plus reminder-event read model, surfaces the operator-facing view in `Recipients`, mirrors the same timing data in `Wellness Activity > Reminder Policies`, and summarizes the active `Next Run Window` in the execution contract panel. Supporting source-of-truth wording is synchronized in `docs/functional-specification.md` and `docs/wellness-program-execution-checklist.md`. Verification passed with `npm run build` and `npm run backend:typecheck`.
- `2026-09-04`: a dedicated wellness reporting reference baseline now exists to guide the next OHIH analytics slice before code changes expand dashboards or exports. `docs/wellness-reporting-reference.md` now defines the current delivered baseline, the normalized outcome taxonomy (`Completed`, `Deferred`, `Dismissed`, `TimedOut`, `InProgress`, `NoInteraction`), recommended KPI definitions, proposed reporting surfaces, and phased implementation guidance that separates backend/frontend-only reporting work from later agent-contract cleanup. `docs/open-questions-and-challenges.md` now also records the remaining ambiguity around `Close` versus `Completed` semantics so future analytics work does not silently assume the wrong behavior. Verification passed through cross-document review against the current wellness monitoring, event model, and roadmap entries.
- `2026-09-04`: the wellness reporting reference is now decomposed into an implementation-ready execution plan. `docs/wellness-reporting-task-breakdown.md` now splits the work into `Phase A` (backend/frontend-only reporting baseline), `Phase B` (agent contract cleanup that requires rebuild/package work), and `Phase C` (advanced analytics), with concrete workstreams for metric normalization, read models, API contracts, UI surfaces, exports, and verification. `docs/wellness-reporting-reference.md` now links to that execution breakdown so future implementation can start from an agreed task map instead of reinterpreting the reporting goals from scratch. Verification passed through cross-document review confirming the task breakdown remains aligned with the approved reporting reference and the existing open-question boundary for `Close` versus `Completed`.
- `2026-09-04`: OHIH reporting Phase C workstreams C1-C3 are implemented. The backend now returns normalized device-local occurrence rows and program comparison dimensions, while the Reports screen provides family, cadence, distribution, site, area, hourly, daily/weekly, strongest-window, and guided-routine depth analytics with tabular alternatives. Partial completion was deliberately not introduced: started-but-not-completed is abandonment and step advancement remains progress evidence. Verification passed with backend typecheck, production frontend build, targeted ESLint, 12/12 wellness tests, a live read-only PostgreSQL smoke across 4 programs and 53 occurrences, and authenticated browser QA covering distribution comparison, weekly switching, open Select state, zero-defer handling, and a no-page-overflow 390 px viewport. C4 active-user audit rollups remain privacy-gated and Phase B real-device close verification remains deferred by the user.
- `2026-09-04`: the Wellness Program detail route was repaired after browser verification exposed `ReferenceError: CANCELLABLE_STATUSES is not defined`. Cancellable lifecycle states now have one shared frontend owner in `src/lib/notification-status.ts`, consumed by Notification Center, Wellness Programs list, and Wellness Program detail instead of duplicated or undeclared route constants; the summary cards also constrain long execution labels without page-level horizontal overflow. The unrelated development-only hydration metadata warning remains non-blocking. Verification passed with the focused notification-status unit test, targeted ESLint, production frontend build, and browser reload of the affected detail URL without the missing-constant exception.
- `2026-09-05`: the Wellness Program authoring identity and timezone controls were refined from operator feedback. The multi-device picker, selected chips, confirmation, and detail summary now show hostname as the primary identity while retaining UUID device IDs only in selection state and API payloads. The shared wellness schedule form now uses the authored Radix Select owner with fixed `UTC-12` through `UTC+14` labels backed by valid IANA fixed-offset identifiers, and legacy regional timezone values are normalized when a draft is reopened. Verification passed with three focused frontend tests, targeted ESLint, production frontend build, Windows `TimeZoneInfo` resolution for the emitted `Etc/GMT-8` value, and authenticated browser checks covering the open timezone popup, keyboard selection, hostname-only device selection, and legacy detail rendering as `MTI-NB-373` plus `UTC+8`. The strict premium audit remains at the existing 33 repository-wide findings and introduced no new finding category for this refinement.
- `2026-09-04`: Phase A wellness reporting is complete without changing the Windows Agent contract. The backend exposes `GET /communications/{communicationId}/wellness-reporting` plus `GET /dashboard/wellness-program-rollups`, normalizes raw reminder events into outcome reporting semantics including explicit `AmbiguousCloseCompletion` handling, retains site and area context, and uses active policies as the documented display-rate denominator. The frontend surfaces `Outcome Funnel`, `Action Breakdown`, per-device `Last Terminal Outcome`, a global Wellness report with family/status/site/area/date filters and a complete-versus-defer trend, plus program-summary, device-outcome, and event-timeline CSV exports. Source-of-truth docs are synchronized in `docs/functional-specification.md`, `docs/openapi.yaml`, `docs/wellness-reporting-reference.md`, and `docs/wellness-reporting-task-breakdown.md`. Verification passed with `npm run test:wellness-reporting` (7 tests), targeted ESLint, `npm run backend:typecheck`, `npm run backend:build`, `npm run build`, browser review against a deterministic local API fixture, and a read-only real-PostgreSQL smoke that loaded four wellness programs and verified both rollup and detail reporting shapes.
- `2026-09-04`: OHIH reporting Phase B implementation is complete up to the rollout gate. `OQ-16` is resolved so `GotIt` and `Done` are completion, `RemindMeLater` is defer, `Start` and `Next` are progress, and all close-only paths are dismissal with no separate `Skipped` bucket. The Windows Agent now emits `Close` as `Dismissed` plus explicit terminal metadata, while backend reporting retains the ambiguity bucket only for historical `Completed + Close` evidence. Verification passed with a zero-warning Release agent build, all 34 agent tests, all 7 npm wellness-reporting tests, backend typecheck, and targeted ESLint. A signed production-profile `1.0.12` MSI candidate was built successfully with SHA-256 `1B1BF0CE7EF73D485F34AEAEC3B6B7DEDAA76932B6959D56C43F517B8F8C2B79`; it has not been uploaded or rolled out, so real-device close-path verification remains pending.
- `2026-08-31`: the Docker admin publish path upload ceiling is now aligned with the MSI rollout workflow. `docker/nginx.admin-gateway.conf` now raises `client_max_body_size` from `10m` to `512m`, matching the backend upload route baseline for `POST /devices/rollout-packages/upload` so operator MSI uploads from `Settings > Desktop Agent` no longer fail at the gateway with `413 Request Entity Too Large` before reaching the backend service. Supporting operational notes were updated in `README.md`, `docs/deployment-and-environment.md`, and `docs/operational-runbook.md`. Verification: source inspection confirms the gateway limit now matches the backend upload baseline; automated `docker compose config` verification could not be executed in the current agent environment because the `docker` CLI is unavailable here.
- `2026-08-31`: the public rollout package path is now wired end-to-end instead of only existing as generated metadata. `backend/src/modules/agent/controller/register-agent-routes.ts` now serves `GET /agent/packages/local/{fileName}` by reading the published MSI from `backend/local-packages/` through `DeviceActionService`, and `backend/src/app/bootstrap/create-backend-app.ts` now passes the same package-management service into the agent route module. `docker/nginx.admin-gateway.conf` now also proxies `/agent/packages/*` to the backend service so public rollout URLs on the admin host do not fall through to the frontend SSR `404` page. Supporting deployment and operations notes were updated in `README.md`, `docs/deployment-and-environment.md`, and `docs/operational-runbook.md`. Verification passed with `npm run backend:build`; live public-link verification still requires restarting the backend plus reloading the gateway in the target environment.
- `2026-08-31`: the Docker package store is now made durable instead of depending on the backend container filesystem. `docker-compose.yml` now mounts the named volume `backend_local_packages` into `/app/backend/local-packages`, and deployment notes were updated in `README.md`, `docs/deployment-and-environment.md`, plus `docs/operational-runbook.md` so rebuild behavior is explicit for rollout-package uploads. This addresses the observed production symptom where an uploaded `MTI.Alert.Agent.Setup.msi` disappeared after backend rebuild because no persistent storage layer had been attached yet. Verification passed through source inspection of the compose mount and volume declaration; live persistence validation still requires recreating the backend container and re-uploading at least one package on the target host.
- `2026-08-31`: the package upload path is now hardened for the new Docker volume-backed package store. `backend/src/modules/devices/service/device-action-service.ts` now routes package publication through a helper that falls back from `rename` to `copyFile + rm` when Docker mounts place `/tmp` and `/app/backend/local-packages` on different filesystems (`EXDEV`). This addresses the observed `500 Internal Server Error` during `POST /api/devices/rollout-packages/upload` after enabling the persistent package volume. Verification passed with `npm run backend:build`; live upload retry is still required on the target host after redeploying the backend container.
- `2026-09-01`: Windows Agent packaging now separates environment configuration from version metadata cleanly. `MTI.Alert.Agent/Installer/build-agent-package.ps1` now accepts `-PackageVersion <major.minor.patch>` and stamps the same version into the tray build, updater build, and MSI `ProductVersion` without writing version data into `appsettings.json`. `MTI.Alert.Agent/MTI.Alert.Agent.csproj`, `MTI.Alert.Agent/Updater/MTI.Alert.Updater/MTI.Alert.Updater.csproj`, `MTI.Alert.Agent/AssemblyInfo.cs`, `MTI.Alert.Agent/Installer/MTI.Alert.Agent.Setup/Package.wxs`, and `MTI.Alert.Agent/Installer/MTI.Alert.Agent.Setup/MTI.Alert.Agent.Setup.wixproj` were aligned so runtime server settings remain baked through `appsettings.json` while package versioning flows through build metadata only. Supporting notes were added to `docs/deployment-and-environment.md` and `docs/operational-runbook.md`. Verification passed with `powershell -ExecutionPolicy Bypass -File ".\\MTI.Alert.Agent\\Installer\\build-agent-package.ps1" -EnvironmentProfile Local -PackageVersion "1.0.8"`, followed by file-version inspection of the built tray and updater binaries (`1.0.8.0`) plus MSI `ProductVersion` inspection (`1.0.8`).
- `2026-09-01`: MSI upgrade behavior is now aligned with environment-specific agent packaging. `MTI.Alert.Agent/Installer/MTI.Alert.Agent.Setup/Package.wxs` now removes the previously installed `appsettings.json` during install before re-applying the packaged payload, preventing Windows Installer from preserving a stale `Program Files\MTI\MTI.Alert.Agent\appsettings.json` across environment-switching upgrades. This addresses the observed runtime where version `1.0.8` installed successfully but the tray still displayed `127.0.0.1:4019` because the old local package baseline file remained in place. Supporting deployment guidance was updated in `MTI.Alert.Agent/docs/client/deployment-and-rollout.md`. Verification target: rebuild a production MSI after this installer change, reinstall it on the test machine, and confirm the installed `Program Files` `appsettings.json` now matches the packaged production `Server:BaseUrl`.
- `2026-09-01`: the managed bootstrap rollout path now includes a startup-script-friendly install helper for large on-prem `MSI` deployment. `MTI.Alert.Agent/Installer/install-agent-if-missing.vbs` checks uninstall registration for `MTI Alert Agent`, exits cleanly when the product is already present, and otherwise runs a silent local `msiexec /i ... /qn /norestart` install against a previously staged package. Supporting rollout guidance was synchronized in `MTI.Alert.Agent/docs/client/deployment-and-rollout.md` and `docs/deployment-and-environment.md`. Verification passed with `cscript.exe //nologo ".\\MTI.Alert.Agent\\Installer\\install-agent-if-missing.vbs" /?`, which returned the expected usage output after the new bootstrap helper was added.
- `2026-09-01`: the bootstrap install helper was hardened again after live startup-script testing exposed first-run failures on real user machines. `MTI.Alert.Agent/Installer/install-agent-if-missing.vbs` now creates nested bootstrap log folders recursively and normalizes nullable uninstall-registry `DisplayName` values before comparison, preventing `Path not found` and `Invalid use of Null: 'CStr'` runtime crashes during the pre-install check. Verification passed with `ProgramData` redirected to a local test directory plus `cscript.exe //nologo ".\\MTI.Alert.Agent\\Installer\\install-agent-if-missing.vbs" ".\\does-not-exist.msi"`, which now completes the installed-product registry scan and exits cleanly through the already-installed branch instead of crashing in folder creation or registry scanning, and `cscript.exe //nologo ".\\MTI.Alert.Agent\\Installer\\install-agent-if-missing.vbs" /?`, which still returns the expected usage output.
- `2026-09-01`: Windows Agent trust handling now supports a server-owned pending enrollment queue so newly installed endpoints do not require manual pre-registration one by one. A new migration `backend/migrations/0016_phase4_device_enrollment_approvals.up.sql` adds `device_enrollment_requests`; `POST /agent/session` now records unknown endpoints as pending enrollment attempts and returns a trust-gated conflict response; backend device routes now expose `GET /devices/pending`, `POST /devices/pending/{requestId}/approve`, and `POST /devices/pending/{requestId}/reject`; and the admin `Devices` page now separates approved devices from a `Pending Approval` queue with site-aware approval controls. Documentation is synchronized in `docs/openapi.yaml`, `docs/functional-specification.md`, `docs/technical-implementation-plan.md`, `docs/database-schema-specification.md`, and `docs/security-and-access-model.md`. Verification target: run the new migration, complete backend/frontend typecheck and build validation, then confirm a fresh GPO-installed endpoint appears in the pending queue, approve it from the admin UI, and verify the agent connects successfully on its next retry without reinstall.
- `2026-09-02`: Windows Agent updater startup handling was hardened for standard-user rollout and tagged for package version `1.0.9`. `MTI.Alert.Agent/Updater/MTI.Alert.Updater/Services/StartupTaskManager.cs` now registers the `MTI Alert Agent` logon task from XML using the built-in `Users` group principal with `LeastPrivilege` instead of `schtasks /RU INTERACTIVE`, avoiding machine-or-user-specific task ownership that blocked non-admin laptops from manually or service-triggered task use. `MTI.Alert.Agent/Updater/MTI.Alert.Updater/Services/ActiveUserSessionLauncher.cs` was added so the updater service can relaunch the tray directly into the active interactive session after rollout, while `RolloutExecutionWorker` now prefers that session-aware launch path before falling back to the scheduled task. The agent, updater, WiX packaging defaults, and `build-agent-package.ps1` baseline were all advanced to `1.0.9`, and rollout guidance was synchronized in `MTI.Alert.Agent/docs/client/deployment-and-rollout.md` plus `docs/deployment-and-environment.md`. Verification target: `dotnet build` the updater, agent, tests, and WiX projects; run the test suite including the new startup-task XML principal coverage; then build a production MSI `1.0.9` and validate that a standard user receives tray startup at logon without requiring local administrator rights.
- `2026-09-02`: Windows Agent rollout packaging now produces a directly usable signed MSI instead of stopping at an unsigned build artifact. `MTI.Alert.Agent/Installer/build-agent-package.ps1` now reuses `prepare-agent-rollout-package.ps1` after the WiX build so the package is automatically Authenticode-signed and metadata-validated using signing configuration from the repo `.env` unless `-SkipSign` is requested explicitly. This closes the admin UI rollout blocker where a fresh rebuild could appear as `Version = -` and `Signature = -` because backend package inspection correctly rejected unsigned MSI output. Verification passed with `powershell -ExecutionPolicy Bypass -File ".\\MTI.Alert.Agent\\Installer\\build-agent-package.ps1" -EnvironmentProfile Production -ServerBaseUrl "https://mtialert.merdekabattery.com/" -PackageVersion "1.0.9"`, which now returns `SignatureStatus = Valid`, `SignatureThumbprint = 0DCBA67CE24D09DCC98EFFC6E5F85146C1B30630`, and `PackageSha256 = 678C5B2A86AB80B9E9D604EFC764C6145C30D1873BDC845F7DC6F622C3B8F0D9`; and with `prepare-agent-rollout-package.ps1 -SkipSign` against `backend/local-packages/MTI.Alert.Agent.Setup.msi`, which now resolves local package metadata as version `1.0.9` plus a valid signer thumbprint for the rollout dialog.
- `2026-09-02`: local Desktop Agent package upload now supports in-place replacement for rebuilt MSI artifacts that keep the same published file name and version. `backend/src/modules/devices/service/device-action-service.ts` now reuses the existing package entry when the hash matches, or replaces the stored MSI when the uploaded hash differs, instead of returning `409 PACKAGE_UPLOAD_CONFLICT`. The upload response now reports `replacedExisting` so the admin settings UI can distinguish a no-op reupload from a real overwrite. `docs/openapi.yaml` and `src/routes/_app.settings.tsx` were synchronized accordingly. Verification target: upload the same signed MSI twice and confirm `alreadyExists = true`, then upload a rebuilt MSI with the same name/version and confirm the package registry refreshes to the new `sha256` without forcing a file rename or version bump.
- `2026-09-02`: the Desktop Agent package registry now also supports explicit package removal from the admin UI. `backend/src/modules/devices/controller/register-device-routes.ts` adds `DELETE /devices/rollout-packages/local/{fileName}`, backed by `device-action-service.deleteLocalPackage(...)`, which removes the MSI from `backend/local-packages` and records an audit entry. `src/routes/_app.settings.tsx` now renders a `Remove` action beside each published package so operators can delete an attachment and immediately reupload a rebuilt MSI with the same file name/version when backend runtime caching or replacement semantics lag behind expectations. `src/services/devices.service.ts`, `src/types/index.ts`, and `docs/openapi.yaml` were synchronized. Verification target: delete a published MSI from `Settings > Desktop Agent`, confirm it disappears from the registry after refresh, then upload the same file name again successfully.
- `2026-09-02`: local package inspection no longer depends on the backend process working directory. `backend/src/modules/devices/service/device-action-service.ts` now resolves both `backend/local-packages` and `MTI.Alert.Agent/Installer/prepare-agent-rollout-package.ps1` relative to the module file location instead of `process.cwd()`, which previously caused the rollout dialog to fall back to blank metadata (`Version = -`, `Signature = -`, `Thumbprint = -`) whenever the backend was launched from a nested working directory such as `backend/`. Verification passed with `npm run backend:typecheck`, republishing `MTI.Alert.Agent.Setup.msi` into the canonical `backend/local-packages` folder, and a direct `prepare-agent-rollout-package.ps1 -SkipSign` inspection that now returns version `1.0.9`, thumbprint `0DCBA67CE24D09DCC98EFFC6E5F85146C1B30630`, and `SignatureStatus = Valid` for the same package path used by the admin rollout registry.
- `2026-09-02`: rollout package metadata now has a filesystem fallback that survives runtimes where Windows-native inspection is unavailable or fails. `MTI.Alert.Agent/Installer/build-agent-package.ps1` now emits a canonical signed MSI plus adjacent `.rollout.json` metadata manifest, while `backend/src/modules/devices/service/device-action-service.ts` now reads those manifests first and can also infer version plus signer thumbprint from the published MSI file name before falling all the way back to hash-only inspection. This reduces blank package-registry rows on non-Windows or mismatched backend runtimes and gives operators a self-describing artifact they can publish directly. Verification passed with a fresh production build for `1.0.9`, backend typecheck/build, and publication of the canonical MSI plus manifest files into `backend/local-packages`.
- `2026-09-02`: production rollout publication is now being standardized around a canonical file name plus authoritative metadata instead of proliferating file names per version. `backend/src/modules/devices/service/device-action-service.ts` now keeps the published file name stable, accepts optional authoritative package metadata during upload, validates any supplied `sha256` against the actual uploaded content, and persists a `.rollout.json` sidecar manifest for the canonical package. `backend/src/modules/devices/controller/register-device-routes.ts` now accepts `X-Package-Metadata` as base64 JSON on `POST /devices/rollout-packages/upload`. `MTI.Alert.Agent/Installer/build-agent-package.ps1` now supports `-UploadProduction`, resolves upload auth from explicit parameters or repo `.env`, signs the MSI, writes the canonical manifest, logs in to the admin API when necessary, and uploads the exact signed `MTI.Alert.Agent.Setup.msi` plus authoritative metadata to the production package registry. Supporting docs were synchronized in `docs/openapi.yaml`, `docs/deployment-and-environment.md`, and `MTI.Alert.Agent/docs/client/deployment-and-rollout.md`. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, and `powershell -ExecutionPolicy Bypass -File ".\\MTI.Alert.Agent\\Installer\\build-agent-package.ps1" -EnvironmentProfile Production -ServerBaseUrl "https://mtialert.merdekabattery.com/" -PackageVersion "1.0.9" -SkipSign`, which now emits canonical package output plus `.rollout.json` metadata ready for direct upload; live `-UploadProduction` execution still requires a reachable authenticated admin API session in the target environment.
- `2026-09-02`: the remaining production-package localhost regression was traced to two overlapping rollout issues and is now documented with explicit evidence. First, `MTI.Alert.Agent/Installer/MTI.Alert.Agent.Setup/MTI.Alert.Agent.Setup.wixproj` now honors `/p:UseExternalPublishPayloads=true`, preventing the WiX build from republishing the source-controlled local `appsettings.json` over the already prepared production payload. Second, packaging diagnostics now surface MSI `ProductCode`, `UpgradeCode`, and `PackageCode` through `prepare-agent-rollout-package.ps1`, `build-agent-package.ps1`, and the `.rollout.json` manifest so operators can distinguish a rebuilt package from an older same-version install. Verification evidence: the fixed production publish output under `MTI.Alert.Agent/artifacts/environment-builds/Production/Release/agent/win-x64/appsettings.json` contains `https://mtialert.merdekabattery.com/`; a fresh rebuild via `build-agent-package.ps1 -EnvironmentProfile Production -ServerBaseUrl "https://mtialert.merdekabattery.com/" -PackageVersion "1.0.9"` completed successfully and produced `MTI.Alert.Agent.Setup.msi` at `2026-09-02 14:36:31` with `ProductCode {DBCE1E73-AD9E-4CDC-B2D1-D450AE9B43C5}`, `UpgradeCode {82D68627-53CD-4E81-9FF4-C5A59AB2B7A3}`, `PackageCode {EE14A029-08E9-4BDD-8557-765B67AD2704}`, `SignatureStatus = Valid`, and adjacent manifest `MTI.Alert.Agent.Setup.msi.rollout.json`; and the currently installed endpoint still reports uninstall entry `{A628758D-1F4F-417F-9489-F378AA0511A3}` plus `C:\Program Files\MTI\MTI.Alert.Agent\appsettings.json` last write `2026-08-28 11:08:50`, proving the machine was still on the pre-fix `1.0.9` package when the localhost screenshot was captured. Supporting rollout docs were synchronized in `docs/deployment-and-environment.md` and `MTI.Alert.Agent/docs/client/deployment-and-rollout.md`.
- `2026-09-02`: the `MTI Alert Agent` startup task disappearance after rollout was traced to invalid Task Scheduler XML emitted by the updater service. Runtime updater logs on the affected machine showed the exact sequence `Ensuring startup task removal on service stop` followed by `schtasks create failed ... ERROR: The task XML contains a value which is incorrectly formatted or out of range. (17,26):LogonType:Group`, which left the task missing even though `MTI.Alert.Updater` restarted successfully. `MTI.Alert.Agent/Updater/MTI.Alert.Updater/Services/StartupTaskManager.cs` now keeps the `Users` group principal plus `LeastPrivilege` but omits the rejected `<LogonType>Group</LogonType>` node from the generated XML, and `MTI.Alert.Agent/Tests/MTI.Alert.Agent.Tests/StartupTaskManagerTests.cs` was updated accordingly. Verification passed with `dotnet test .\\MTI.Alert.Agent\\Tests\\MTI.Alert.Agent.Tests\\MTI.Alert.Agent.Tests.csproj --no-restore --filter StartupTaskManagerTests` and `dotnet build .\\MTI.Alert.Agent\\Updater\\MTI.Alert.Updater\\MTI.Alert.Updater.csproj -c Release --no-restore`.
- `2026-09-02`: the startup-script bootstrap helper now also records successful executions to the on-prem shared drive for rollout follow-up. `MTI.Alert.Agent/Installer/install-agent-if-missing.vbs` now appends a best-effort line to `\\10.60.10.44\apps$\mti-alert-bootstrap-success.log` on exit code `0`, covering both `already installed` and `installation verification succeeded` outcomes. Each appended line includes timestamp, hostname, resolved username (interactive signed-in user when available, otherwise the current execution account), and status; failures to write to the UNC share are logged locally under `%ProgramData%\MTI Alert\Bootstrap\Logs\install-agent-if-missing.log` but do not flip the bootstrap result to failure. Supporting rollout guidance was synchronized in `MTI.Alert.Agent/docs/client/deployment-and-rollout.md`. Verification passed with `cscript.exe //nologo ".\\MTI.Alert.Agent\\Installer\\install-agent-if-missing.vbs" /?`; a sandboxed dry-run also confirmed the success branch attempts to append to the configured UNC path, although the actual network write could not be completed inside the current sandbox policy.
- `2026-09-03`: registered device visibility now includes best-effort Active Directory enrichment for the latest active Windows user without collapsing the platform into a person-centric trust model. `backend/migrations/0017_phase4_device_directory_enrichment.up.sql` adds runtime snapshot columns to `public.devices`; `backend/src/modules/auth/service/ldap-authenticator.ts` now supports service-account directory lookup; `backend/src/modules/agent/service/agent-service.ts` persists `Employee`, `NonEmployee`, or `Unknown` device-user snapshots during agent session and active-user changes; `backend/src/modules/devices/service/device-read-service.ts` exposes those fields through `GET /devices`; and `src/services/devices.service.ts`, `src/types/index.ts`, plus `src/routes/_app.devices.tsx` now surface current user and department details in the admin device list while preserving existing assigned-owner data. Supporting docs were synchronized in `docs/openapi.yaml`, `docs/database-schema-specification.md`, `docs/technical-implementation-plan.md`, `docs/architecture-decisions.md`, `docs/integration-contracts.md`, and `docs/windows-agent-client-specification.md`. Verification passed with `npm run backend:typecheck`, `npm run backend:build`, and `npm run build`; plus `npm run backend:migrate:status:dev`, which confirmed the new migration file is recognized and currently `pending` rather than silently applied. Live LDAP resolution against a running agent session still requires applying `0017_phase4_device_directory_enrichment.up.sql` and exercising a device in the target environment.
- `2026-09-03`: the first live device-user enrichment test exposed a remaining client-side gap: the backend was ready to persist `last_active_user_identifier`, but the Windows tray app never actually populated `ActiveUserIdentifier` on `/agent/session` or `/agent/heartbeat`, so the admin device list showed `Current User = -` and `Unknown` for online agents. `MTI.Alert.Agent/Services/ActiveUserContextResolver.cs` now resolves the interactive Windows username, while `MTI.Alert.Agent/Services/SessionService.cs` and `MTI.Alert.Agent/Services/HeartbeatService.cs` now send that value on session creation and heartbeat requests. `MTI.Alert.Agent/Tests/MTI.Alert.Agent.Tests/ActiveUserContextAgentRequestTests.cs` now covers both request paths so the regression is pinned. Verification target: build the Windows agent test project, run the new focused tests, then reconnect an agent and confirm `GET /devices` begins returning `lastActiveUserIdentifier` plus AD-enriched fields for live machines.
- `2026-09-03`: the publishable Windows Agent shell is now trimmed for production use. `MTI.Alert.Agent/MainWindow.xaml` no longer renders the internal `Wellness Preview` shortcut panel in the main tray window, which removes lab-only quick triggers from the production-facing surface without changing the underlying wellness execution engine or server-driven reminder paths. Supporting client roadmap evidence was synchronized in `MTI.Alert.Agent/docs/client/implementation-roadmap.md`. Verification passed with `dotnet build .\\MTI.Alert.Agent\\MTI.Alert.Agent.csproj -c Release --no-restore`.
- `2026-09-03`: rollout verification exposed a stale-version bug in backend device visibility. The Windows Agent already reports `agentVersion` on heartbeat, and the published `1.0.11` payload correctly embeds `FileVersion 1.0.11.0` plus `ProductVersion 1.0.11+...`, but `backend/src/modules/agent/service/agent-service.ts` previously updated `agent_version` only during `/agent/session`, not `/agent/heartbeat`. That meant endpoints upgraded in-place could keep showing `1.0.10` in the admin device list until the session was recreated. `reportHeartbeat()` now applies `input.agentVersion` as well, so version visibility advances on the first post-upgrade heartbeat. Verification passed with `npm run backend:typecheck` and `npm run backend:build`; the built production agent artifact was also inspected directly and confirmed to carry `FileVersion 1.0.11.0`.
